# Reports Table PR-1

This PR is intentionally small and low risk. It fixes the report table pagination arrow directions, preserves RTL mirroring, adds regression coverage, and records the audit gap matrix. It does not change APIs, accounting logic, report calculations, database state, or export behavior.
