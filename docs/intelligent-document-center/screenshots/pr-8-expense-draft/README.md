# PR-8 Screenshot Review Notes

- `rtl-desktop-ready-light.png`: شاشة مراجعة document `expense` عربية RTL في الوضع الفاتح؛ يظهر زر **إنشاء مسودة مصروف** في موضع إجراء واضح، مع حالة `جاهزة للمسودة` ودليل الرأس المراجع.
- `rtl-desktop-dialog-light.png`: حوار إنشاء المسودة يعرض سبب القرار، حساب مصروف، تصنيف، مركز تكلفة، وطريقة دفع فقط. الرسالة توضح أن المبلغ والضريبة يأتيان من الدليل المراجع ولا يمكن تعديلهما في الحوار. لم تُعرض حقول مبلغ أو ضريبة أو نطاق أو payload خام.
- اللقطات من fixture واجهة محلي في وضع المعاينة، وليست دليل اتصال API Laravel أو اختبار E2E للخلفية.

- `rtl-desktop-linked-light.png`: بعد إجراء fixture الواجهة، استبدل CTA برابط **فتح مسودة المصروف EXP-DRAFT-0417** وشارة مسودة؛ كما ظهر حدث `expense_draft_created` في سجل العرض.
- `rtl-mobile-linked-dark.png`: فحص بصري جيد للاتجاه RTL والتباين الداكن وعدم وجود تمرير أفقي، لكنه يكشف أن لقطة سياق جديد عادت إلى الحالة الجاهزة وCTA الإنشاء. ستُعاد اللقطة باسم حالة صحيح بعد جعل script يبني رابط Expense داخل سياق الجوال نفسه.

- استُبدلت `rtl-mobile-linked-dark.png` بلقطة صحيحة تنشئ رابط Expense داخل سياق الجوال ذاته؛ الرابط والرقم والشارة ظاهرون مع تخطيط RTL داكن بلا تمرير أفقي.
- `ltr-desktop-linked-dark.png`: واجهة Document Center بالإنجليزية LTR والوضع الداكن؛ يظهر رابط **Open expense draft EXP-DRAFT-0417** وحقول دليل Expense بعناوين إنجليزية، مع بقاء بيانات fixture العربية كبيانات تجريبية فقط.

- `rtl-desktop-draft-created-read-only-light.png`: لقطة تصحيح PR #448 من fixture واجهة محلي بعد الحالة `draft_created`. يظهر رابط **فتح مسودة المصروف EXP-DRAFT-0417** وشارة المسودة والمعاينة الآمنة والسجل؛ لا يظهر تنبيه «أكمل المطابقات…» ولا زر إنشاء/تعديل/تأكيد/رفض/حل/إعادة تحقق/إكمال مراجعة. تحقق السكربت من هذه الغيابات قبل الالتقاط؛ اللقطة دليل واجهة محلي وليست اتصال Laravel E2E.
