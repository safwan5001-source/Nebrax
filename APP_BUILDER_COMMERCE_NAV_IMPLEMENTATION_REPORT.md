# تقرير تنفيذ: إدراج App Builder ضمن التجارة الإلكترونية

## الخلاصة

تم نقل رابط **App Builder** الموجود مسبقاً من مجموعة المبيعات إلى مجموعة **التجارة الإلكترونية** في القائمة الجانبية الرئيسية. لم تتغير وظيفة App Builder أو مساراته أو نموذج التفويض الخاص به. فُتح [PR #980](https://github.com/safwan5001-source/Nebrax/pull/980) على `main`، ولم يُنفّذ Merge أو Deploy.

## 1. السبب الجذري

كان مسار `/app-builder` مسجلاً في مجموعة `sales` رغم أن القدرة مرتبطة تجارياً بالمفتاح `commerce.app_builder`، ورغم أن App Builder ينتمي من ناحية المعلومات المعمارية إلى مساحة التجارة الإلكترونية. لذلك كان موضع الرابط لا يعكس البنية المفاهيمية المطلوبة.

## 2. التنفيذ الدقيق

أزيلت خانة `/app-builder` من مصفوفة `sales` وأضيفت إلى مصفوفة `ecommerce` بعد رابط `/commerce`. استُخدم نمط القائمة الحالي دون إنشاء مكوّن أو أسلوب تنقل جديد.

حُفظت الحراستان الموجودتان على العنصر دون تعديل:

- `appKey: 'commerce.app_builder'` لإخفاء الرابط عندما تكون القدرة التجارية غير مرئية للمؤسسة.
- `permission: 'apps_builder.view'` لإخفاء الرابط عن المستخدم غير المصرح له.

حُدّثت تسمية الرابط في رسائل الشريط إلى `صانع التطبيقات` بالعربية و`App Builder` بالإنجليزية. بقي رابط الوجهة `/app-builder` كما هو، ولذلك لم يتغير التفويض المباشر للمسار أو العزل بين المستأجرين.

## 3. الملفات التي تغيّرت

| الملف | التغيير |
|---|---|
| `web/src/components/layout/sidebar.tsx` | نقل عنصر App Builder من `sales` إلى `ecommerce` مع إبقاء `appKey` و`permission`. |
| `web/src/messages/ar.json` | تغيير تسمية الشريط إلى `صانع التطبيقات`. |
| `web/src/messages/en.json` | تغيير تسمية الشريط إلى `App Builder`. |
| `web/src/components/layout/__tests__/commerce-workspace-nav.test.ts` | إضافة اختبار يثبت الموضع الفعلي ويحافظ على حراستي القدرة والصلاحية. |

لم تتغير ملفات App Builder أو قاعدة البيانات أو السلوك المالي والمحاسبي.

## 4. موضع التنقل

الموضع النهائي هو:

> التجارة الإلكترونية / E-commerce → `/commerce` → صانع التطبيقات / App Builder → `/app-builder`

العنصر ليس وحدة عليا جديدة، وليس تابعاً لـ Store Appearance أو Store Builder. ويستخدم أيقونة `Smartphone` الموجودة مسبقاً، مع نفس نمط المسافات والحالة النشطة واتجاه RTL/LTR المستخدم في Sidebar.

## 5. التحقق من RBAC وعزل المستأجرين

لم تُنشأ صلاحية جديدة ولم تُخفّف أي حراسة. بقيت صلاحية `apps_builder.view` مرتبطة بالعنصر، وبقي مفتاح القدرة التجاري `commerce.app_builder` مرتبطاً به. كما أن اختبار صفحة App Builder الحالي يثبت أن المستخدم الذي يفتقد `apps_builder.view` لا يستدعي API ويشاهد رسالة عدم الوصول. يظل التفويض المباشر للمسار من مسؤولية الخادم حتى عند إخفاء العنصر من القائمة، ولم يتغير أي كود خادمي أو كود عزل المستأجرين.

## 6. الاختبارات المنفذة والنتائج

نجحت الاختبارات المركزة التالية: اختبارات تنقل Commerce، واختبارات صلاحيات الواجهة، واختبارات صفحة App Builder، واختبارات نموذج تنقل Commerce، واختبارات مساعدات App Builder. النتيجة هي **5 ملفات اختبار ناجحة و35 اختباراً ناجحاً**.

نجح `npm run build` للإنتاج.

أما `npx tsc --noEmit` فأظهر **12 خطأ موجوداً مسبقاً في 9 ملفات اختبار غير متعلقة بهذا التغيير**، ولم يظهر خطأ في الملفات المعدلة.

أما `npm run lint` فلم يكتمل لأن `next lint` فتح معالج إعداد ESLint التفاعلي لعدم وجود إعداد مكتمل في المشروع. أُلغي المعالج دون إنشاء ملفات إعداد أو تعديل المشروع.

## 7. حالة Build وCI

حالة البناء المحلي: **ناجح**.

حالة فحص TypeScript: **محجوب بأخطاء baseline غير مرتبطة** كما هو موضح أعلاه.

حالة lint: **غير مكتمل بسبب معالج إعداد ESLint التفاعلي الموجود مسبقاً**.

حالة PR: مفتوح، و`mergeStateStatus` هو `UNSTABLE` وقت إنشاء التقرير. لم يُنفّذ Merge أو Deploy.

## 8. Base SHA

`08c143b614be7f2b303528a17a13e862c08b6261`

## 9. Head SHA

Commit التنفيذ:

`5179c8eadd008e1946ba2f28d5b9e7cc6857701a`

سيضاف هذا التقرير نفسه في commit لاحق على نفس الفرع، لذلك سيظهر SHA أحدث كرأس PR بعد نشر التقرير.

## 10. الفرع

`awj/app-builder-commerce`

## 11. رقم ورابط PR

[PR #980](https://github.com/safwan5001-source/Nebrax/pull/980)

## 12. المخاطر أو العمل المتبقي

لا توجد مخاطر وظيفية جديدة من هذا التغيير المحدود. يبقى على المشروع، خارج نطاق هذه المهمة، معالجة أخطاء TypeScript الموجودة مسبقاً وإضافة إعداد ESLint غير تفاعلي إذا كان ذلك مطلوباً في بوابة CI. لم يُجرَ أي تعديل على App Builder نفسه، لذلك لا توجد أعمال متبقية خاصة بربط هذا العنصر.

## References

[1]: https://github.com/safwan5001-source/Nebrax/pull/980 "Pull Request #980 — Place App Builder under Commerce"
