# تصميم التصنيفات وإتاحتها في نبراكس

> **الحالة:** تصميم معتمد بقرار مالك المنتج في 20 أغسطس 2026.  
> **القرار:** تعديل التصنيف قبل الترحيل وبعده سياسة أعمال قابلة للضبط من قسم **إعدادات التقارير**، وليست حكماً ثابتاً.

## 1. الهدف

يوفّر نبراكس تصنيفات مُدارة وقابلة للفلترة والتحليل بدل الحقول النصية الحرة المتفرقة، مع إبقاء أثرها **تحليلياً فقط**: لا تغيّر التصنيفات حساباً أو مبلغاً أو قيداً مرحّلاً. تستلهم التجربة فصل تصنيف العميل عن الوسم، والتصنيف الهرمي للمنتج، والفلترة والتجميع حسب التصنيف في تقارير المبيعات كما توثقه دفترة [1] [2] [3].

## 2. نطاق أنواع التصنيف

| النطاق | مصدر الحقيقة | علاقة الكيان | مستوى الشجرة | حالة التنفيذ المستهدفة |
|---|---|---|---|---|
| العملاء | تصنيف مُدار جديد | تصنيف عميل واحد لكل عميل | مسطّح | استبدال النص الحر مع ترحيل آمن للبيانات القائمة |
| الموردون | تصنيف مُدار جديد | تصنيف مورد واحد لكل مورد | مسطّح | جديد، ومستقل عن تصنيف العميل للطرف ذي النوع `both` |
| المنتجات والخدمات | `ProductCategory` الحالي | تصنيف منتج واحد | هرمي | يبقى كما هو؛ يضاف إلى المرشحات والتقارير |
| فواتير المبيعات | تصنيف مستند جديد | تصنيف فاتورة واحد | مسطّح | جديد |
| فواتير المشتريات | تصنيف مستند جديد | تصنيف فاتورة شراء واحد | مسطّح | جديد |
| المصروفات | `ExpenseCategory` الحالي | تصنيف مصروف واحد | مسطّح | يبقى كما هو؛ يوسّع التصفية والتحليل حيث توجد تقارير |
| سندات القبض | تصنيف مستند جديد | تصنيف سند قبض واحد | مسطّح | جديد |
| سندات الصرف | تصنيف مستند جديد | تصنيف سند صرف واحد | مسطّح | جديد |

لا تدمج تصنيفات المنتجات أو المصروفات القائمة في جدول جديد؛ فهي عقود مُنفذة ومختبرة، وإعادة هيكلتها لا تضيف قيمة وظيفية لهذه المهمة. تُضاف التصنيفات الجديدة في كيان مخصص (`classifications`) يحمل نطاقاً واضحاً، ثم تحفظ المعرّفات على الكيانات ذات الصلة. هذا يمنع العدّ المزدوج الذي قد ينشأ من علاقة متعددة القيم أثناء تجميع التقارير المالية.

## 3. نموذج البيانات

### 3.1 جدول `classifications`

يحمل: `id`، `tenant_id`، `branch_id`، `scope`، `name`، `description`، `is_active`، والطوابع. المجالات المسموح بها هي: `customer`، `supplier`، `sales_invoice`، `purchase_invoice`، `receipt`، و`payment`.

النموذج `BranchScoped` مع عزل مشروط معقول: تصنيفات العملاء والموردين تتبع مفاتيح مشاركة أطرافها؛ أما تصنيفات المستندات فهي تشغيلية للفروع. في كل الحالات، علاقة التصنيف المخزّنة على مستند قائم تُحلّ كمرجع خارج Scope الفرع حتى لا تختفي حجة تاريخية من تقرير أو عرض مستند.

### 3.2 المراجع على الكيانات

| الكيان | العمود المقترح | النطاق المسموح | قاعدة صحة البيانات |
|---|---|---|---|
| `partners` | `customer_classification_id` | `customer` | يتاح للعميل أو `both` |
| `partners` | `supplier_classification_id` | `supplier` | يتاح للمورد أو `both` |
| `invoices` | `classification_id` | `sales_invoice` | تصنيف فاتورة المبيعات |
| `purchases` | `classification_id` | `purchase_invoice` | تصنيف فاتورة المشتريات |
| `payments` | `classification_id` | `receipt` أو `payment` بحسب `direction` | يمنع عدم تطابق الاتجاه والنطاق |

تبقى `products.category_id` و`expenses.category_id` كما هما. ويظل عمود `partners.classification` محفوظاً مؤقتاً لتوافق القراءة والترحيل من البيانات التاريخية، لكنه لا يعود مدخلاً جديداً بعد اكتمال الترحيل.

## 4. سياسة تعديل التصنيف في إعدادات التقارير

تضاف مجموعة `reports` إلى `Settings::DEFAULTS` بمفتاحين منطقيين:

| المفتاح | الافتراض | المعنى |
|---|---:|---|
| `allow_classification_edit_before_post` | `true` | يسمح بإسناد أو تعديل التصنيف للمسودة قبل الترحيل. |
| `allow_classification_edit_after_post` | `false` | يسمح بتعديل التصنيف التحليلي لمستند مرّحل. |

تظهر السياسة في صفحة مستقلة داخل **إعدادات التقارير → سياسة التصنيفات**. تعرض الشاشة حالتي السماح بوضوح، وتوضح أن السماح بعد الترحيل يغيّر تصنيف المستند في التقارير اللاحقة وقد يغيّر نتائج المقارنة لفترة تاريخية؛ أما منعه فيثبت البعد التحليلي كما كان عند الترحيل. لا يخفي المنع التصنيف ولا يعطّل المرشح؛ بل يعطّل حقل التحرير في المستند مع سبب ظاهر، وفق مبدأ أن الشاشة تقول الحقيقة قبل المحاولة.

> **حدود السياسة:** لا تسمح السياسة مطلقاً بتعديل المبالغ أو البنود أو القيود أو الحسابات بعد الترحيل. هي تتحكم في بُعد تحليلي فقط، ويظل القيد المالي immutable.

تُقرأ السياسة من خدمة واحدة مسؤولة عن التحقق من قابلية التعديل، لا من المتحكم أو الواجهة وحدهما. ويُضاف اختبار لكل قيمة من القيمتين.

## 5. الفلترة والتقارير

تضيف تقارير المبيعات المرشحات التالية: تصنيف العميل، تصنيف المنتج، وتصنيف فاتورة المبيعات. وتضيف تقارير المشتريات: تصنيف المورد، تصنيف المنتج، وتصنيف فاتورة الشراء. ويضاف نوع عرض تجميعي للتصنيف حيث لا يحوّل التجميع قيمة رأس فاتورة إلى مبلغ مكرر؛ لذلك يكون تجميع تصنيف الفاتورة على رؤوس الفواتير، وتجميع تصنيف المنتج على سطورها فقط.

تتوسع تقارير العملاء والمخزون والمصروفات وسندات القبض/الصرف في أي مسار تقرير موجود فعلاً بالمشروع. لا تنشأ تقارير وهمية لوحدة لا تملك تقريراً تشغيلياً، لكن كل شاشة قائمة أو تقرير يدعم الكيان يحصل على مرشح التصنيف المناسب. يجب أن تمر المرشحات إلى CSV/PDF بنفس الاستعلام حتى لا يختلف التصدير عن الشاشة.

## 6. الترحيل والتوافق الرجعي

تنشئ الهجرة تصنيف عميل من كل قيمة نصية فريدة موجودة في `partners.classification` وتربط العميل بها. لا يُحذف النص الحر في الهجرة نفسها كي لا تنكسر واجهة أو تكامل قائم؛ يصبح حقلاً انتقالياً للقراءة فقط ثم يُزال في مهمة توافق لاحقة بعد تأكيد الاستبدال. لا تُنشأ تصنيفات للموردين من هذه القيم من دون دليل دلالي أن النص كان تصنيف مورد؛ الطرف `both` لا يُنسخ تصنيفه تلقائياً إلى المورد لتجنّب تقرير شرائي مضلّل.

لا يُعاد ترحيل أو إعادة قيد أي مستند موجود. المستندات السابقة تبقى قابلة للعرض بغير تصنيف إلى أن يقرر مستخدم مخوّل تصنيفها، وحينها تطبق سياسة ما بعد الترحيل الجديدة.

## 7. مصفوفة الاختبار والقبول

| المسار | التحقق الإلزامي |
|---|---|
| الإدارة | إنشاء/تعديل/تعطيل/حذف آمن؛ منع حذف تصنيف مستخدم؛ اسم فريد داخل النطاق المرئي؛ عزل مستأجر وفرع. |
| الإسناد | رفض نطاق لا يطابق الكيان؛ منع التصنيف غير النشط في مستند جديد؛ إبقاء مرجع التصنيف التاريخي قابلاً للعرض. |
| السياسة | تعديل المسودة يمر أو يرفض بحسب الإعداد؛ تعديل مستند مرّحل يمر أو يرفض بحسب الإعداد؛ لا يتغير القيد في الحالتين. |
| التقارير | مرشحات العميل/المورد/المنتج/المستند تعطي نتائج صحيحة؛ تجميع الفاتورة لا يكرر الإجماليات؛ التصدير يطابق الشاشة. |
| الترحيل | التصنيفات النصية القائمة للعملاء تتحول إلى صفوف مُدارة غير مكررة؛ لا تفقد بيانات. |
| الواجهة | حالات تحميل وفشل وفراغ؛ تركيز ظاهر وتنقل لوحة مفاتيح؛ عربية وإنجليزية متطابقتان؛ تعطيل مفهوم عند منع التعديل. |

## المراجع

[1]: https://docs.daftra.com/user_manual/%d8%aa%d8%b5%d9%86%d9%8a%d9%81-%d8%a7%d9%84%d8%b9%d9%85%d9%84%d8%a7%d8%a1-%d9%88%d9%83%d9%8a%d9%81%d9%8a%d8%a9-%d8%a7%d9%84%d8%a7%d8%b3%d8%aa%d9%81%d8%a7%d8%af%d8%a9-%d9%85%d9%86%d9%87/
[2]: https://docs.daftra.com/user_manual/%d9%83%d9%8a%d9%81%d9%8a%d8%a9-%d8%aa%d8%b5%d9%86%d9%8a%d9%81-%d8%a7%d9%84%d9%85%d9%86%d8%aa%d8%ac%d8%a7%d8%aa/
[3]: https://docs.daftra.com/tutorial/%d8%b9%d8%b1%d8%b6-%d8%aa%d9%82%d8%b1%d9%8a%d8%b1-%d9%85%d8%a8%d9%8a%d8%b9%d8%a7%d8%aa-%d8%a7%d9%84%d8%a8%d9%86%d9%88%d8%af-%d8%ad%d8%b3%d8%a8-%d8%a7%d9%84%d8%aa%d8%b5%d9%86%d9%8a%d9%81/
