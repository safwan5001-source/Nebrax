# نبراس ERP — سياق المشروع

نظام محاسبي سحابي **ERP متعدد المستأجرين (SaaS)** بنموذج اشتراكات، مرجعه الوظيفي دفترة.
**اللغة:** تواصل بالعربية. الواجهات RTL أولاً.

## الحالة الحالية

**الـ backend مكتمل لـ v1 ومختبَر** على SQLite وPostgreSQL في CI:
- النواة: محرك القيد المزدوج + عزل multi-tenant + دليل حسابات سعودي.
- المستندات: الأطراف/المنتجات، الفوترة، المشتريات، المرتجعات (مبيعات/مشتريات).
- النقد: المدفوعات (قبض/صرف) + التسوية + التخصيص polymorphic (متعدد الفواتير/المشتريات).
- المخزون الدائم (متوسط متحرك) + قيد تكلفة البضاعة المباعة التلقائي.
- الموارد البشرية (HR): الموظفون + مسيّر رواتب شهري (إنشاء → ترحيل الاستحقاق → الصرف) + الاستقطاعات (GOSI/سُلف).
- التقارير: ميزان المراجعة، قائمة الدخل، الميزانية، كشف الأستاذ، كشف حساب الطرف، أعمار الديون، وتقارير العملاء (المبيعات، الأرصدة، المقبوضات، والمواعيد)، وتقارير المخزون (لقطة القيمة، أرصدة المخازن الكمية، الحركة الدائمة، الأذون المرحّلة، وفروق الجرد المرحّلة).
- ZATCA: المرحلة 1 (QR/TLV) + المرحلة 2 جزئياً (UUID + ICV + سلسلة هاش PIH + UBL).
- REST API كامل (Sanctum + RBAC بالأدوار + عزل tenant) + طبقة الاشتراكات وحدود الخطط وتحصين المصادقة. لكل مستأجر UUID تقني داخلي ورقما حساب ودعم رقميان دائمان؛ يحجزهما `TenantReferenceNumberService` من عدّاد مقفول منفصل، فلا يعيدان استخدام قيم حذف مستقبلي ولا يقبلان التحرير الجماعي. توجد كذلك مساحة تشغيل داخلية للمنصة (`/api/platform/*` و`/platform`) بحساب `PlatformAdministrator` مستقل؛ تعرض مؤشرات مجمّعة للقراءة فقط ولا تمرّ عبر `SetTenant`، وتبحث بهذه الأرقام ضمن المصادقة فقط. من تفاصيل المستأجر فيها، تُدار الإسنادات التجارية عبر معاينة صريحة ثم خدمات الاستحقاق القائمة، وتُعرض الحالة التجارية والوصول الفعلي وحالة التشغيل منفصلةً. يسجل `PlatformSubscription` الإيراد الشهري المتعاقد عليه بالهللات للعقود النشطة؛ وهو ليس فاتورةً أو تحصيلاً نقدياً.

**الواجهة (web/) قيد البناء** — Next.js 15، RTL، متصلة بالـ API: دخول، لوحة تحكم،
الفواتير (قائمة/إنشاء/تفاصيل + ZATCA QR)، العملاء/الموردون (CRUD + كشف حساب)، المدفوعات،
المشتريات، المرتجعات، نقطة البيع (POS)، الموارد البشرية (HR: موظفون + مسيّر رواتب)، التقارير، الإعدادات/الاشتراك.

**المؤجَّل بصراحة:** ZATCA م2 الكاملة (توقيع/CSID/إرسال — يحتاج شهادات وبيئة آمنة)،
اختبارات آلية للواجهة، استضافة الـ backend (Laravel) خارج Vercel.

## الـ Stack

- **Backend:** Laravel 11 + PostgreSQL (الإنتاج) / SQLite (الاختبار)
- **Auth:** Sanctum (JWT) + RBAC
- **Frontend (لاحقاً):** Next.js 15 + TypeScript + Tailwind + shadcn/ui — RTL
- **Cache/Queue:** Redis + Horizon. **Storage:** S3/R2.

## قواعد معمارية غير قابلة للكسر

1. **القيد المزدوج صارم:** كل معاملة = قيد متوازن (Σ مدين = Σ دائن). غير المتوازن يُرفض.
1. **النقود بالـ minor units (هللات) كـ `bigint`.** ممنوع `float`/`double` في أي حساب مالي. 100.50 ريال = 10050.
1. **القيود immutable بعد الترحيل.** التصحيح بقيد عكسي عبر `LedgerService::reverse()` فقط.
1. **العزل تلقائي:** كل نموذج أعمال يرث `BaseModel` (فيه `TenantScope` + `BelongsToTenant`). ممنوع استعلام يدوي يتجاوز الـ scope.
1. **عزل الفروع إلزامي:** كل نموذج جديد يُصنَّف صراحةً — `BranchScoped` (تشغيلي معزول، الافتراضي) أو `BelongsToBranch` (محاسبي بتصفية صريحة) أو `CompanyWide` (مشترك). نموذج غير مصنَّف **يُفشل الـ CI** (`BranchIsolationGuardTest`). المرجع الكامل: `design-system/foundations/multi-branch-architecture.md`.
1. **السياسة تُضبط ولا تُفرَض:** نبراكس يخدم +٦٠ نشاطاً، فما هو «أفضل ممارسة» لنشاطٍ قد يكون خطأً لآخر. أي قرار له **أكثر من طريقة معقولة وصحيحة** (سياسة عمل لا صحّة تقنية) يُبنى خياراتٍ يحكمها إعدادٌ في قسمه (`Settings`/`BranchSettings`)، بافتراضٍ يحمي المستأجرين القائمين — لا سلوكاً واحداً مفروضاً ولا ترقيعاً لاحقاً. **عند الشكّ في التصنيف: اعرض القرار على المالك قبل التنفيذ.** المرجع الكامل والقائمة: `design-system/foundations/configurable-policies.md`.
1. **الحسابات التجميعية (`is_group`) لا تقبل قيوداً مباشرة.**
1. **`TenantContext` مسجّل singleton** في `TenancyServiceProvider` — حاسم للعزل.
1. **PSR-4:** كلاس واحد لكل ملف.

## بنية الملفات الأساسية

```
app/Models/            BaseModel, Tenant, User, Account, JournalEntry, JournalLine, AccountBalance,
                       Partner, Product, Invoice/InvoiceLine, Payment/PaymentAllocation,
                       Purchase/PurchaseLine, ReturnDocument/ReturnLine, StockMovement
app/Tenancy/           TenantContext, TenantScope, BelongsToTenant
app/Services/Accounting/  LedgerService (المحرك), ChartOfAccountsSeeder, InvoiceService,
                          PaymentService, PurchaseService, ReturnService, InventoryService, ZatcaService
app/Services/Reporting/   ReportService (ميزان/دخل/ميزانية/أستاذ/كشف طرف/أعمار)
app/Support/           ApplicationCatalog, Money, Rbac, Plans, PlanGate
app/Http/Middleware/   SetTenant, EnsurePermission, EnsurePlatformAdministrator, EnsureActiveSubscription, EnforcePlanLimit, ForceJsonResponse
app/Http/Controllers/Api/  Auth, PlatformAuth/PlatformDashboard (تشغيل داخلي منفصل), Partner, Product, Account, Invoice, Payment, Purchase, Return, Report
app/Http/Requests/ · app/Http/Resources/   (تحقق المدخلات + تنسيق المخرجات)
app/Providers/         TenancyServiceProvider
routes/api.php         كل مسارات الـ REST API
database/migrations/   000001..000081 (النواة + المستندات + المخزون + الاشتراكات + ZATCA + مديرو المنصة)
tests/Feature/         LedgerTest + اختبارات كل وحدة + اختبارات API (مصادقة/RBAC/عزل)
web/                   واجهة Next.js 15 (انظر DESIGN_SYSTEM.md)
.github/workflows/     ci.yml (php على sqlite+pgsql) · web-ci.yml (بناء Next.js)
```

> **ملف `DESIGN_SYSTEM.md`** في الجذر = المرجع الإلزامي لتصميم الواجهة (مع هذا الملف).
> **بيئة الاختبار:** المستودع نواة فقط؛ يُبنى مشروع Laravel كامل عبر `setup.sh`/CI لتشغيل الاختبارات.

## خريطة الوحدات (Module Map)

الخريطة الوظيفية الكاملة لنبراس، مجموعةً في 8 أقسام بالترتيب. الشريط الجانبي في `web/`
يعكس هذه المجموعات حرفياً. الوحدات المُعلَّمة **(جاهزة)** لها backend مكتمل وشاشة واجهة؛
البقية روابط في الشريط فقط (الشاشات الداخلية قيد البناء تدريجياً).

**المبيعات**
- الفواتير وعروض الأسعار **(جاهزة)** · نقاط البيع POS **(جاهزة)** · العروض والخصومات · الأقساط · المبيعات المستهدفة والعمولات · الفاتورة الإلكترونية (ZATCA م1+م2)

**العملاء**
- متابعة العملاء **(جاهزة)** · نقاط الولاء · الاشتراكات والعضويات · النقاط والأرصدة · التأمينات

**المخزون**
- إدارة المنتجات · المشتريات **(جاهزة)** · إدارة الموردين · دورة المشتريات · الأذون المخزنية · إدارة الجرد · تقارير المخزون **(جاهزة: قيمة/أرصدة/حركة/أذون/فروق جرد)**

**الحسابات**
- دليل الحسابات · إدارة الأصول · مراكز التكلفة · دورة الشيكات

**المالية**
- المصروفات **(إصدار تشغيلي أول: تصنيفات، بائع/مورّد، مرفقات، ضريبة، مركز تكلفة، مسودة وترحيل)** · سندات القبض **(إصدار تشغيلي أول: قائمة، مسودة، تخصيص فاتورة، عرض وترحيل)** · خزائن وحسابات بنكية **(إصدار تشغيلي أول: خزينة/بنك مرتبطان بحساب أستاذ، رئيسي، تعطيل وحذف آمن، تحويل داخلي وقيد متوازن، سياسة رصيد سالب قابلة للضبط)** · عُهَد الموظفين **(مسودة وصرف مرحّل من خزينة/بنك إلى حساب عُهَد الموظفين، وعرض/مطبوعات/نسخ وحذف آمن؛ تسوية أحادية السطر لعهدة مرحّلة بنوع نشط، مدين الحساب المختار ودائن 1160، مع سجل وقيد متوازنين)** · إعدادات المالية **(مركز إعدادات، تصنيفات مصروفات، وأنواع تسوية مشتركة للمؤسسة مع تعطيل آمن للنوع المستخدم)**

**الموارد البشرية**
- شؤون الموظفين **(جاهزة)** · الورديات وأدوار الصلاحيات **(جاهزة)** · الحضور والانصراف **(جاهزة)** · إدارة المرتبات · إدارة العقود · إدارة الطلبات

> **معمار «الموظف والمستخدم والأدوار» مكتمل** (`design-system/foundations/hr-users-architecture.md`): ربط `users.employee_id`، ونموذج إضافة موحّد، والورديات، والحضور (`BranchScoped`، ساعات مشتقّة)، وجدول أدوار قابل للضبط لكل مؤسسة يقود `EnsurePermission` (بديلاً عن `Rbac::MATRIX` الثابتة، مع سقوطٍ آمنٍ عليها وزرعٍ طبق الأصل للتوافق الرجعي). شاشة الأدوار تبويبٌ في `/hr` لـ`owner`/`admin`.

**التشغيل والعمليات**
- أوامر الشغل · دورة العمل · الحجوزات · إدارة الإيجارات والوحدات · عقود الإيجار · تتبع الوقت · إدارة التصنيع

**اللوجستيات** (ميزة تنافسية لنبراس)
- إدارة الأسطول · أوامر الشحن

**الإعدادات**
- التقارير **(جاهزة)** · إعدادات الضرائب **(جاهزة)** · إعدادات الحساب **(جاهزة)**

> **ملاحظة:** شاشتا **المدفوعات** و**المرتجعات** مبنيّتان بالكامل وتظلّان متاحتين بمساراتهما
> (`/payments`، `/returns`)؛ تُدمجان لاحقاً ضمن تدفّقات المبيعات/الحسابات في الشريط.

## كتالوج التطبيقات — المرحلة الأولى

`App\Support\ApplicationCatalog` هو مصدر حقيقة **نطاق المنتج** للتطبيقات: مفتاح ثابت، مجموعة نبراكس، نضج القدرة (`built` / `coming_soon` / `retired`)، إلزامية، واعتماديات صريحة. لا يخزن هذا الكتالوج قرار مستأجر بتفعيل تطبيق، ولا يستبدل RBAC أو الخطط أو فاحص الجاهزية؛ تلك مراحل لاحقة منفصلة عمداً.

لا يجوز جعل قدرة `coming_soon` قابلة للتفعيل أو تخزين حالة `true` لها. كما لا تنقل حالات المفاتيح في حساب دفترة مرجعي إلى افتراضات مستأجري نبراكس. اختبار `ApplicationCatalogTest` يحرس مفاتيح الكتالوج واعتمادياته وصحة عقده.

**P1 — التمكين المحروس (مبنية):** `App\Services\TenantApplicationService`
يحوّل الكتالوج الثابت إلى قرار فعلي لكل مستأجر عبر جدولي `tenant_application_states`
(القرار الحالي) و`tenant_application_events` (سجل تدقيق ثابت لا يُحدَّث). التفعيل يتحقق
من النضج والاعتماديات؛ الإيقاف يتحقق من الإلزامية والتوابع المفعّلة. المسارات
`GET /applications`، `POST /applications/enable`، `POST /applications/disable` محروسة
بصلاحيتَي `apps.view`/`apps.manage` (owner/admin افتراضياً). واجهة `/applications` في
`web/` تستهلكها (جدول + شارات حالة + حوار تفعيل/إيقاف بسبب اختياري).

**P2 — إيقاف واعٍ بالبيانات (مبنية):** `disable()` يفحص وجود بيانات تشغيلية حقيقية
(`PosSession`, `CrmActivity`, `StockMovement`, `Purchase`, `Employee`, `Expense`/`Payment`/
`EmployeeCustody`, فرع غير رئيسي) قبل الإيقاف؛ إن وُجدت تتحوّل الحالة إلى `suspended`
(قراءة فقط) بدل `disabled` الكاملة. `compliance.zatca` مستثناة (بياناتها أعمدة على
`Invoice` المملوكة لقدرة إلزامية، لا نموذج مستقل لها).

**P2 — الإنفاذ الفعلي + تفاعل الشريط الجانبي (مبنية):** `EnsureApplicationActive`
middleware مُطبَّق على مسارات القدرات الثماني القابلة للإيقاف — `enabled` يمرّ،
`suspended` قراءة فقط (GET/HEAD)، `disabled` يمنع كل شيء. **استثناء متعمَّد:** مسارات
مرجعية يستهلكها ميزات إلزامية أخرى (`GET branches`، `GET warehouses`، `GET employees`)
تبقى بلا حجب — حجبها كان سيكسر اختيار الفرع/المخزن/البائع في الفوترة بمجرد تعطيل قدرة
اختيارية غير مرتبطة. `TenantApplicationService::ENFORCEMENT_CUTOVER_AT` يحمي المستأجرين
القائمين: مسجَّل قبل هذا التاريخ = مفعَّل تلقائياً لأي قدرة لم يقرر بشأنها؛ مستأجر جديد
بعده يبدأ بالافتراضي الحقيقي (معطّلة حتى التفعيل الصريح). الشريط الجانبي يقرأ
`GET /applications/nav-state` (بلا RBAC — كل الأدوار تحتاجه) ويُخفي عنصر/مجموعة `sidebar.tsx`
المرتبطة بـ`appKey` معطّل، ويُظهرها فور التفعيل.

**P3 — إدارة تجارية من المنصة (مبنية):** صفحة تفاصيل المستأجر في منصة التشغيل تعرض ملخص الخطة والمنتجات المشمولة والإضافات والتجارب والاستحقاقات القديمة والسجل المنتهي، مع فصل الحالة التجارية عن نتيجة الاستحقاق وعن `TenantApplicationState`. يستدعي التعيين المعاينة المركزية ثم `CommercialAssignmentService` أو مسار التجارب القائم فقط؛ لا توجد كتابة مباشرة على جداول الإسنادات أو المنح أو حالة التطبيق. تُربط إجراءات الإلغاء والسحب والجدولة بمسار المستأجر المحدد، بينما تبقى حالة التشغيل للقراءة فقط لأن تغييرها من اختصاص إدارة المستأجر وقواعد `TenantApplicationService` المحمية.

**صراحة خارج النطاق حتى اليوم:**
- حالة "يحتاج تهيئة" (`needs_configuration`) والتمييز ثنائي المستوى بين تجميد جزئي
  وقراءة فقط كاملة — لا قدرة مبنية اليوم لها مثال حقيقي يبرر أيّاً منهما.
- توسيع نطاق الإنفاذ ليشمل مرتجعات المشتريات/إشعارات مدين/الموردين تحت `purchases.cycle`،
  أو الخزائن والحسابات البنكية تحت `finance.operations` — الأولى لم تُتحقَّق مساراتها بعد،
  والثانية مرجع مشترك مع سندات القبض/الصرف غير المرتبطة بأي مفتاح كتالوج.

## المحرك — العقد (Contract)

```php
app(LedgerService::class)->post(array $lines, array $meta): JournalEntry;
// $lines: [['account_id'=>uuid,'debit'=>int,'credit'=>int,'partner_type'=>?,'partner_id'=>?], ...]
app(LedgerService::class)->reverse(JournalEntry $e, ?string $date, ?string $reason): JournalEntry;
```

أي وحدة جديدة (فاتورة/دفعة/مشترى) **تولّد قيودها عبر هذا المحرك** — لا كتابة مباشرة في journal_lines.

## أوامر متحقَّقة

```bash
php artisan migrate:fresh        # تهيئة القاعدة
php artisan test --filter=LedgerTest   # 5 اختبارات يجب أن تكون خضراء
php artisan serve
```

**قبل أي commit: شغّل `php artisan test` ويجب أن تنجح كل الاختبارات.**

## دليل الحسابات (الأكواد المرجعية)

1110 الصندوق · 1120 البنك · 1130 العملاء · 1140 المخزون · 1150 ضريبة مدخلات
2110 الموردون · 2120 ضريبة مخرجات · 2130 رواتب مستحقة · 3110 رأس المال
4110 إيرادات المبيعات · 5110 تكلفة البضاعة · 5120 الرواتب والأجور · 5140 الوقود

## بيانات شركة المرجع (نبراس الطموح — للاختبار)

الرقم الضريبي 15 خانة · العملة SAR · ضريبة VAT 15% · المدن: الدمام، الجبيل، الخبر، الظهران، الأحساء.

## مبادئ العمل

- بناء **وحدة وحدة**، كل واحدة قابلة للتشغيل والاختبار قبل التالية.
- تشخيص المشكلة منهجياً قبل تعديل الكود.
- مراجعة يدوية للتغييرات — لا وضع autonomous على كود محاسبي حسّاس.
- تجنّب الأنماط الزائدة؛ كود نظيف قابل للصيانة، معماري لا مبتدئ.

## قواعد التحقق المحاسبي الإلزامية

هذه القواعد **مُلزِمة ذاتياً في كل مهمة** — تُطبَّق دون طلب على كل ما بُني سابقاً وكل ما سيُبنى:

1. **مرور إجباري عبر المحرك:** كل وحدة تولّد أثراً محاسبياً (فاتورة، دفعة، مشترى، مرتجع، تسوية) يجب أن تمرّ عبر `LedgerService::post` حصراً. **ممنوع منعاً باتاً** الكتابة المباشرة في `journal_lines` أو `journal_entries`.
1. **الإجماليات مشتقة لا مُدخلة:** كل مبلغ إجمالي (`subtotal`, `tax_amount`, `total`) يُحسب من السطور (line items)، ولا يُدخله المستخدم يدوياً إطلاقاً. تُعاد الإجماليات من السطور (مصدر الحقيقة) **قبل توليد أي قيد**.
1. **توازن وربط:** كل قيد متوازن (Σ مدين = Σ دائن)، ومربوط بمصدره عبر `source_type` و`source_id` ليكون له أثر رجعي كامل.
1. **النقود بالهللات:** كل المبالغ `bigint` بالهللات (minor units)، ممنوع `float`/`double` في أي حساب مالي.
1. **العزل:** كل نموذج أعمال يرث `BaseModel` ويُعزل تلقائياً بالـ tenant.
1. **بروتوكول ما قبل أي Pull Request (إلزامي):**
   - شغّل `php artisan test` **كاملاً** (لا `--filter`)، ويجب أن تكون **كل** الاختبارات خضراء.
   - ثم اكتب في الرسالة **القيد المحاسبي الناتج لكل عملية مالية جديدة** في الوحدة (الحسابات، المدين، الدائن) بصيغة **جدول واضح** للمراجعة قبل الموافقة على الدمج.
   - **لا تطلب فتح PR قبل عرض القيود الناتجة.**

## طرق الدفع — نطاق تشغيلي

- `PaymentMethod` إعداد `CompanyWide` للمؤسسة: يربط كل طريقة بخزينة أو حساب بنكي، ويمكن تعطيله أو تعيينه افتراضياً. لا تُحذف طريقة استُخدمت في سند؛ تعطّل فقط.
- عند تهيئة مؤسسة بلا طرق دفع، تنشأ مرة واحدة: **تحويل بنكي** و**نقدي** و**شيك** و**بطاقة ائتمان**. يربط النقد بالخزينة الرئيسية، وتربط الطرق الثلاث الأخرى بالحساب البنكي الرئيسي. لا تعاد طريقة حذفها يدوياً ما دام للمؤسسة أي طريقة قائمة.
- ينتقي سند الدفع طريقة نشطة، ثم يلتقط **اسمها** ويربط السند بالخزينة أو الحساب البنكي الموافق لنوعها. تعديل الطريقة لاحقاً لا يغير السند السابق.
- **مؤجل صراحةً إلى مهمة محاسبية مستقلة:** رسوم البوابات، ضريبة القيمة المضافة على الرسوم، قواعد قابلية الخصم، حساب مصروف الرسوم، وصافي تسوية البوابة أو أي تغيير في قيود القبض والصرف.
- تظل قيود القبض والصرف القياسية قائمة من دون توسعة: قبض = مدين البنك/الصندوق ودائن العملاء؛ صرف = مدين الموردين ودائن البنك/الصندوق.

## الواجهة (web/) — قواعد موجزة

- المرجع الإلزامي: **`DESIGN_SYSTEM.md`** (أداة محاسبية نظيفة كثيفة، الجداول هي البطل، لون هوية واحد، خطّان).
- الستاك: Next.js 15 (App Router) + TS + Tailwind + مكوّنات بأسلوب shadcn، RTL أولاً، next-intl، next-themes.
- تتصل بالـ REST API عبر `web/src/lib/api.ts` (Bearer token). الأرقام تُعرض من الهللات → ريال في طبقة العرض فقط.
- التحقق: `npm run build` يجب أن ينجح؛ Web CI يفرضه على كل تغيير في `web/`.

## الترقيم التسلسلي

كل أرقام المستندات تُولَّد من مصدر واحد: `App\Support\GeneratesDocumentNumbers` (trait على النموذج).
**ممنوع** كتابة منطق ترقيم جديد في خدمة أو متحكّم — النموذج يعلن تصنيفه الفرعي، ومنه يُشتقّ نطاق التسلسل:

- `CompanyWide` → تسلسل واحد للمؤسسة · وما عداه ممّا يوسم بالفرع → تسلسل مستقلّ لكل فرع.
- النمط `max()+1` لا `count()+1` (لا يتصادم بعد الحذف)، وقفلٌ على مِرساة موجودة حتماً (صفّ الفرع أو المستأجر).
- القيد الفريد `(tenant_id, branch_id, number)` + فهرس جزئي للصفوف بلا فرع (`NULL ≠ NULL` في الفهارس).
- **`invoices.zatca_icv` خارج هذه الطبقة نهائياً:** عدّاد سلسلة إصدار ZATCA، ليس رقم مستند.
  لا يُدرج في `GeneratesDocumentNumbers` مهما بدا متسقاً — نطاقه يُقرأ من `ZatcaIcvScope` وحده.
  - الافتراض `tenant` ولا يتغيّر لمستأجر قائم أو جديد. و`branch` خيارٌ **محروس** لا مفتاح حرّ.
  - ⚠️ ICV مرتبط **برقم ضريبي مسجَّل لدى الهيئة** لا بموقع جغرافي: سلسلتان تحت رقمٍ واحد
    مخالفةٌ للمواصفة تُرفض عندها الفواتير في المرحلة ٢. ولأن الضرر يظهر وقت التكامل لا وقت
    الضبط، يُفرض الشرط في الكود لا في الواجهة: `ZatcaIcvScope` يتجاهل المخزَّن إن لم يتحقّق.
  - شرطا إتاحة `branch` (يُفحصان بالاستبطان وقت التشغيل): وجود `branches.vat_number`، وفهرسٌ
    فريد يشمل الفرع على `(tenant_id, branch_id, zatca_icv)`. **كلاهما غير مستوفى اليوم.**

> مقارنة نموذج دفترة (البادئة المتغيّرة · التنسيقات · سلوك العدّاد) بواقع طبقتنا، وما يستحقّ
> الأخذ منه وما يكسر الجوهر: `design-system/foundations/numbering-reference.md`.
> **مصدره غير متحقَّق منه** — اقرأ تحذير §١ فيه قبل نقل أي قيمة إلى كود أو واجهة.
> للتحقق مباشرة من التوثيق الرسمي: صلاحية الوصول لـ `docs.daftra.com` مفعّلة محليًا عبر
> `.claude/settings.local.json` (ملف شخصي غير مُتتبَّع في git، راجع `.gitignore`).

## نطاق مستقبلي (غير منفَّذ)

- **`products.sku` بلا مولِّد تلقائي.** الحقل `nullable` ويُدخل يدوياً. يستحق ترقيماً تلقائياً عبر الطبقة
  الموحّدة أعلاه لأن نقطة البيع تمسحه ضوئياً، فغيابه يعطّل تدفّقاً فعلياً لا شكلياً. (`partners.code`
  اختياري بطبيعته ولا يحتاجه.)
- **إعدادات الترقيم كشاشة واجهة** (بادئة + تنسيق الأرقام لكل نوع مستند) — الطبقة الخلفية جاهزة لها.

## الخطوة التالية

كتالوج التطبيقات مكتمل الآن عبر ثلاث مراحل: P0 (نطاق المنتج) → P1 (التمكين المحروس)
→ P2 (إيقاف واعٍ بالبيانات + إنفاذ فعلي على المسارات + تفاعل الشريط الجانبي). التفصيل
الكامل في قسم "كتالوج التطبيقات" أعلاه، بما فيه ما تبقّى خارج النطاق صراحة.

المتبقي من الخارطة الأعم:
- اختبارات آلية للواجهة (`web/`) — لا توجد اليوم.
- شاشة الإعدادات/الاشتراك.
- تصدير Excel/PDF للتقارير.
- ZATCA المرحلة 2 الكاملة (توقيع/CSID/إرسال — بانتظار شهادات وبيئة آمنة).

> خارطة تشغيل المنصة، وفصل MRR المتعاقد عليه عن الفوترة والتحصيل الفعلي، موثقة في `PLATFORM_DEVELOPMENT_ROADMAP.md`.
