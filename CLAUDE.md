# أَوْج ERP — سياق المشروع

ERP سحابي متعدد المستأجرين، عربي/إنجليزي وRTL أولاً.

**الهوية:** `أَوْج` / `AWJ`. مصدر الاسم المركزي `web/src/lib/brand.ts`، وهوية شركة العميل مستقلة عن المنتج.

**المعمارية:** Laravel 11 + PostgreSQL، Next.js 15 + TypeScript + Tailwind + shadcn/ui. `DESIGN_SYSTEM.md` مرجع التصميم. القيد المزدوج متوازن، الأموال بالهللات bigint، القيود المرحلة immutable، وعزل tenant/branch إلزامي. السياسات ذات البدائل الصحيحة قابلة للضبط. حالة الوحدات يحددها الكود والاختبارات. ZATCA مرحلي ولا تتغير عقوده التقنية بسبب إعادة الهوية.

**إعادة الهوية:** مراجع المنتج المرئية `نبراكس / Nebrax` و`نبراس / Nibras` تنتقل إلى `أَوْج / AWJ`. لا تغيّر تلقائياً أسماء المستودع أو المضيفات أو قواعد البيانات أو متغيرات البيئة أو namespaces أو عقود API. المرجع: `docs/brand/AWJ_REBRAND_AUDIT.md`.
