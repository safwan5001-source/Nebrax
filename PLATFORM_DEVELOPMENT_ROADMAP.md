# خارطة تطوير منصة Nebrax

> **نطاق هذه الخارطة:** تشغيل منصة Nebrax نفسها، لا بيانات ERP الخاصة بأي مستأجر. جميع مؤشرات المنصة يجب أن تكون مجمعة، ومحمية بحساب `PlatformAdministrator` منفصل، ولا تتضمن قائمة أو اسم أو معرّف مستأجر في واجهة التحليلات.

## المبدأ التشغيلي

لوحة المنصة تقيس **صحة المنتج التجاري والتشغيلي**. أما دفتر الأستاذ والفواتير والمدفوعات التابعة للمؤسسة فهي ضمن سياق مستأجر منفصل ولا تُستعلم من مسارات المنصة. أي رقم مالي ظاهر في لوحة المنصة يجب أن يصرّح بمصدره ومعناه: **قيمة متعاقد عليها** أو **فاتورة صادرة** أو **تحصيل نقدي مؤكد**؛ ولا يجوز تسمية أحدها بالآخر.

| مستوى الرقم | مصدر الحقيقة | مثال مؤشر | ما لا يثبته |
|---|---|---|---|
| التعاقد | `platform_subscriptions` | الإيراد الشهري المتعاقد عليه (MRR) | تحصيل نقدي أو دفع فاتورة |
| الفوترة | سجل فواتير المنصة المستقبلي | فواتير اشتراكات صادرة هذا الشهر | سداد العميل |
| التحصيل | سجل مدفوعات/تسويات المنصة المستقبلي | النقد المحصل هذا الشهر | استمرار العقد أو MRR |

## ضوابط العزل غير القابلة للتفاوض

1. تبقى كل مسارات المنصة تحت `/api/platform/*`، وراء `auth:sanctum` و`EnsurePlatformAdministrator`، وخارج `SetTenant` و`SetBranch`.
2. لا تُضاف أي صلاحية منصة إلى أدوار `owner` أو `admin` أو أي دور مستأجر.
3. تعيد واجهات المؤشرات أرقاماً وتجميعات حسب الخطة أو الفترة فقط. لا تعيد أسماء المستأجرين، ولا البريد، ولا المعرّفات، ولا بيانات الوثائق التشغيلية.
4. تحفظ مبالغ المنصة بالهللات (`bigint`) وتُحوّل إلى ريال في طبقة العرض فقط. لا تستخدم المؤشرات `float` أو تحويلات تقريبية.
5. لا يغيّر تسجيل عقد منصة حالة اشتراك المؤسسة أو حدود خطتها تلقائياً. مزامنة الوصول مع الفوترة سياسة مستقلة تُبنى مع سجل تدقيق وقرار صريح.
6. قبل أي تصدير أو تفصيل قد يسمح باستنتاج هوية مؤسسة صغيرة، تطبق المنصة حد إخفاء عينة مناسب، ويُسجل كل طلب في سجل التدقيق.

## المرحلة المنجزة — رؤية تشغيلية أولى

**الهدف:** إنشاء مساحة تشغيلية منفصلة، آمنة، للقراءة فقط.

| التسليم | الحالة | ملاحظات القبول |
|---|---|---|
| حساب `PlatformAdministrator` مستقل | منجز | لا يحمل `tenant_id` ولا يمر عبر RBAC المستأجر. |
| دخول ومسارات منصة منفصلة | منجز | `/platform/login` و`/api/platform/*` مع توكن قراءة محدود. |
| أعداد المستأجرين والمستخدمين | منجز | إجمالي، نشط، غير نشط؛ دون تفاصيل الهوية. |
| سجل اشتراكات المنصة وMRR | منجز | مصدر داخلي لعقود الاشتراك وقيمة شهرية متعاقد عليها بالهللات. |
| تفصيل حسب الخطة وتجديدات 30 يوماً | منجز | تجميعات فقط، مع تنبيه بأن MRR ليس تحصيلاً نقدياً. |

## المرحلة 1 — دورة اشتراك قابلة للتدقيق

**الأولوية:** عالية. تبني مصدر حقيقة تشغيلياً قبل إدخال أي بوابة دفع أو احتساب إيراد محصل.

| التسليم | قرار التصميم | معيار القبول |
|---|---|---|
| كتالوج أسعار الخطط | جدول إصدار أسعار فعّال بالتاريخ والعملة | لا تُستنتج الأسعار من حدود الخطة أو قيم ثابتة في الواجهة. |
| أحداث دورة الاشتراك | سجل immutable: إنشاء، ترقية، تخفيض، إلغاء، انتهاء | يظهر من غيّر الحالة ومتى وسبب التغيير. |
| إدارة العقد | مسار داخلي لإنشاء/إنهاء عقد منصة وتحديثه بلا تداخل زمني | لا يوجد عقدان نشطان متداخلان للمستأجر نفسه. |
| تنبيهات التجديد | قائمة مجمعة لأولوية التجديد خلال 7 و30 و60 يوماً | لا تسرّب أسماء الشركات في شاشة التحليلات. |
| قياسات الاشتراك | MRR، ARR المشتق، تجارب جارية، تحويل تجربة إلى عقد | كل مؤشر يعرّف حالته وتاريخه ومصدره. |

## المرحلة 2 — الفوترة والتحصيل الفعلي

**الأولوية:** عالية بعد اعتماد سياسة الفواتير والضرائب وبوابة الدفع. لا يسمى أي رقم «إيراد محصل» قبل هذه المرحلة.

| التسليم | قرار التصميم | معيار القبول |
|---|---|---|
| فواتير منصة مستقلة | رأس فاتورة وسطور مشتقة من العقد/كتالوج الأسعار | مسودة ثم إصدار؛ رقم فريد وسجل تدقيق. |
| مدفوعات وتسويات | حالة دفع مدفوعة بدليل بوابة أو تحويل يدوي موثق | يمنع التعديل الصامت بعد التأكيد؛ التصحيح بعكس أو إشعار مناسب. |
| تكامل بوابة الدفع | معالج webhook موثوق وموقّع مع idempotency | تكرار الحدث لا يكرر التحصيل أو يغير الحالة خطأ. |
| مؤشرات مالية صحيحة | فواتير صادرة، نقد محصل، متأخرات، تحصيل شهري | فصل واضح بين MRR والفوترة والتحصيل. |
| معالجة الفشل | فشل دفع، إنذارات، إيقاف تدريجي، استرداد | كل انتقال حالة ظاهر وقابل للتدقيق. |

> عند بناء دورة الفوترة والتحصيل، يطبق عقد القيود المحاسبية الخاص بـ Nebrax على أي أثر في الأستاذ؛ لا يُكتب في اليومية مباشرة ولا يُخزن المال إلا بالهللات.

## المرحلة 3 — نمو واحتفاظ المنتج

**الأولوية:** متوسطة. تضاف بعد أن تصبح اشتراكات وفواتير المنصة ذات مصدر موثوق.

| المجال | مؤشرات مجمعة مقترحة | الغرض |
|---|---|---|
| التفعيل | شركات أكملت الإعداد، أول فاتورة، أول مستخدم إضافي | قياس الوصول إلى القيمة الأولى. |
| الاحتفاظ | retained logo، churn، إعادة التفعيل | فهم فقد العملاء دون كشف هوية مؤسسة في لوحة عامة. |
| استخدام المنتج | مستخدمون نشطون أسبوعياً/شهرياً، تبني الوحدات، استعمال السقف | ربط القيمة المدركة بالخطة والتوسع. |
| نمو الإيراد | new MRR، expansion MRR، contraction MRR، churned MRR | شرح التغير في MRR بالسبب لا بالرقم المجرد. |
| القنوات | التحويل بحسب مصدر التسجيل أو شريك البيع | قياس كفاءة الاكتساب عند توافر مصدر موافقة عليه. |

## المرحلة 4 — موثوقية وأمن تشغيل المنصة

**الأولوية:** عالية بالتوازي مع المراحل السابقة.

| التسليم | معيار القبول |
|---|---|
| MFA لمدير المنصة | لا تدخل لوحة المنصة بحساب كلمة مرور فقط في الإنتاج. |
| سجل تدقيق منصة | يسجل الدخول، فشل الدخول، قراءة التقارير، تغيير العقد، والتصدير. |
| إدارة الجلسات | قدرة إبطال التوكنات، انتهاء صلاحية، واسم جهاز/جلسة عند الحاجة. |
| إنذارات تشغيلية | تنبيه عند فشل webhook أو تغير حاد في MRR أو تراكم تجديدات قريبة. |
| لقطات بيانات | materialized aggregates يومية للاستعلامات الثقيلة، مع إعادة بناء قابلة للتدقيق. |
| اختبارات عزل | اختبار إلزامي يثبت أن توكن المستأجر لا يصل لأي API منصة، وأن API المنصة لا يعيد هوية مستأجر. |

## المرحلة 5 — قابلية التوسع وإدارة القرار

**الأولوية:** متوسطة بعد استقرار البيانات التشغيلية.

| التسليم | معيار القبول |
|---|---|
| لوحات زمنية | مقارنة شهرية، اتجاه MRR، وتحليل cohort من لقطات مؤرشفة. |
| تصدير حوكمي | تصدير مجمع ومؤقت مع سجل تدقيق وقيود وصول. |
| صفات وتجارب | feature flags ومنهج تجربة يحدد الفرضية والمقياس وتاريخ الإيقاف. |
| جودة البيانات | مراقبة عقود مفقودة، مبالغ غير صالحة، فجوات تغطية، وعقود متداخلة. |
| هدف مستوى الخدمة | مؤشرات زمن استجابة، أخطاء API، نجاح webhooks، وطابور المعالجة. |

## ترتيب التنفيذ العملي التالي

1. اعتماد كتالوج أسعار داخلي مؤرخ؛ لا تربط السعر بحدود الخطة.
2. إنشاء واجهة داخلية محكومة لإدارة عقود الاشتراك، فوق الأمر التشغيلي الحالي.
3. إضافة سجل أحداث immutable لعقود الاشتراك وتغطية تغييرات الخطة والإلغاء.
4. اعتماد سياسة ضريبة وفوترة وبوابة دفع، ثم بناء الفواتير والتحصيل الفعليين.
5. بناء مؤشرات نمو واحتفاظ من لقطات يومية بعد وصول بيانات الفوترة والتحصيل إلى جودة كافية.

## قائمة قرار قبل كل توسعة

- هل الرقم **عقد** أم **فاتورة** أم **تحصيل**؟ وهل تسميته في الواجهة دقيقة؟
- هل مصدره مسار منصة داخلي، أم هل يستعلم خطأ من بيانات مستأجر تشغيلية؟
- هل يمكن أن يكشف التجميع هوية مؤسسة أو سلوكها عند حجم عينة صغير؟
- هل تغير الحالة أو المالية موثق وقابل للعكس/المراجعة؟
- هل تظل جميع المبالغ أعداداً صحيحة بالهللات من المصدر إلى API؟
- هل اختُبر رفض توكنات المستأجرين ومنع إعادة هوية المستأجر في الاستجابة؟
