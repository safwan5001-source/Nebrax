# تقرير تنفيذ مساحة فواتير المبيعات V2 — طيار واجهة

**المشروع:** نبراس / أَوْج ERP  
**المستودع:** `safwan5001-source/Nebrax`  
**التاريخ:** 2026-09-03  
**الفرع:** `cursor/sales-invoices-workspace-v2-7187`  
**الـ PR:** https://github.com/safwan5001-source/Nebrax/pull/627  
**SHA:** `601acc4ae337adf3eda33ecdd1fa3b2fefc27312`

**القاعدة:** طيار واجهة فقط لشاشة قائمة فواتير المبيعات (`/invoices`). لا إعادة تشغيل للمهمة، لا توسيع لشاشات قوائم أخرى، لا دمج، لا نشر، لا backend/محاسبة/ZATCA/ترقيم/عقد API.

---

## 1. ما تم

صارت `/invoices` مساحة عمل مالية أوضح داخل صدفة أَوْج القائمة:

- عنوان معتمد **فواتير المبيعات** / **Sales Invoices**، وإجراء إنشاء **فاتورة جديدة** / **New Invoice**.
- شريط اكتشاف محلي: بحث أولًا، ثم مدى تاريخ ظاهر، ثم فرز، ثم فلاتر العميل/الحالة/السداد والفلاتر المتقدمة، ثم تصدير CSV وعدد النتائج.
- جدول أكثف بمحاذاة نقدية نهايةً، إجمالي أوضح من المتبقي والأعمدة الداعمة، وحالات نص+شارة (لا لون وحده). **متأخرة** تلميح عرض فقط: مرحّلة + متبقٍّ > 0 + استحقاق أقدم من اليوم.
- نقر الصف أو الرقم يفتح **معاينة خفيفة** من جهة النهاية: حالة المستند، العميل، التواريخ، مبالغ مورد التفاصيل، وسطور مضغوطة.
- إجراءات المعاينة: **فتح الفاتورة**، و**تعديل** إن كانت مسودة حسب القواعد القائمة. القبض والترحيل والمرتجع والحذف والطباعة وZATCA تبقى في صفحة الفاتورة الكاملة.
- تعديل/حذف المسودة ما زالا في صف الجدول مع حوار التأكيد؛ المرحّل مقفل كما كان.
- حالات صريحة: تحميل، خطأ مع إعادة محاولة، قائمة فارغة حقيقية، ولا نتائج مطابقة مع مسح الفلاتر.
- اختصار `/` للانتقال إلى البحث (ما لم يكن المؤشر في حقل أو حوار)، وEscape لإغلاق المعاينة مع إعادة التركيز وحلقة Tab داخل اللوحة.

المراجع (Ramp / Brex / Linear / Stripe / Slash / Column) استُخدمت لكثافة المساحة والتسلسل البصري فقط. توكنز أَوْج والخط والشريط الجانبي وRTL/LTR والفاتح/الداكن بقيت مصدر الحقيقة.

لا شريط مؤشرات مالية ولا تجميع مختلق من الصفحة الحالية — لا يوجد `GET /invoices/summary`.

---

## 2. الملفات

| ملف | الدور |
| --- | --- |
| `web/src/app/(app)/invoices/page.tsx` | مساحة القائمة: شريط محلي، جدول، حالات، معاينة، إجراءات الصف |
| `web/src/app/(app)/invoices/page.test.tsx` | حراسة العنوان والإنشاء والمعاينة الخفيفة والتأخير والفراغ/لا نتائج والقفل |
| `web/src/components/invoices/invoices-list-toolbar.tsx` | شريط اكتشاف محلي لهذه الشاشة عمدًا |
| `web/src/components/invoices/invoice-preview-panel.tsx` | لوحة معاينة خفيفة + تركيز/Escape |
| `web/src/components/invoices/invoice-preview-panel.test.tsx` | أفعال المعاينة، المسودة، Escape، إعادة المحاولة |
| `web/src/lib/invoices/workspace.ts` | اشتقاق العرض: مسودة، تأخير، أعمدة داعمة مخفية افتراضيًا |
| `web/src/lib/invoices/workspace.test.ts` | قواعد العرض دون اختراع حالة مجال |
| `web/src/components/data-table.tsx` | خصائص اختيارية: `onRowClick` / `isRowActive` / `loadingLabel` / `meta.numeric` — القوائم الأخرى بلا تغيير |
| `web/src/components/data-table.test.tsx` | حراسة تفعيل الصف الاختياري |
| `web/src/components/data-explorer/search-bar.tsx` | `inputRef` و`aria-keyshortcuts` اختياريان |
| `web/src/lib/mock-data.ts` | `due_date`، تصفية/ترقيم/فرز ديمو لمسار القائمة فقط |
| `web/src/messages/ar.json` / `en.json` | مفاتيح `invoices.*` لهذه الشاشة. تسمية الشريط `invoicesManage` لم تُمس |

**لم يُمس:** `web/src/components/layout/sidebar.tsx`، صفحات الفاتورة الكاملة، PHP، الهجرات، ZATCA، الترقيم، RBAC، عزل المستأجر.

---

## 3. المكوّنات المعاد استخدامها

`PageHeader` · `DataTable` · `Pagination` · `Badge` · `Button` · `Dialog` · `SearchBar` · `QuickFilters` · `ActiveFilterChips` · `AdvancedFilterDialog` · `ColumnLayoutMenu` · `BranchViewToggle` · `EmptyState` / `ErrorState` / `LoadingState` · `MobileRecordItem` · `formatRiyal`.

لم يُعمَّم شريط الفواتير على قوائم أخرى.

---

## 4. السلوك المحفوظ

- عقد `GET /invoices` و`GET /invoices/:id` كما هما. المبالغ من المورد لا إعادة حساب.
- حالات المستند: `draft` / `posted` / `cancelled`. السداد: `unpaid` / `partial` / `paid`.
- تعديل/حذف المسودة من الصف مع الحوار القائم؛ المرحّل `posted_locked`.
- صلاحيات وعزل المستأجر والفرع عبر الطبقة القائمة.
- الشريط الجانبي: «إدارة الفواتير»، ترتيب أيقونة → تسمية → شيفرون، بلا شريط سفلي جديد.

---

## 5. قرارات واجهة (ضمن توكنز أَوْج)

- الكثافة عبر `py-1.5` في خلايا هذه الشاشة لا إعادة تصميم عام للجدول.
- الإجمالي `font-medium`؛ المتبقي أخفت إلا عند التأخير (`text-warning` مع نص الشارة).
- الأعمدة الداعمة (قبل الضريبة / الضريبة / المدفوع) مخفية افتراضيًا عبر تفضيل الظهور المحلي.
- الصف النشط بـ `bg-primary-soft` أثناء المعاينة.
- نقر الصف يتجاهل الأزرار/الروابط داخل الصف فلا تُسرق إجراءات التعديل والحذف.
- المعاينة `role="dialog"` مع `aria-modal`؛ الخلفية ليست نقطة تبويب.

---

## 6. تحقق الشريط الجانبي

`git diff origin/main -- web/src/components/layout/sidebar.tsx` فارغ. في التشغيل: العنصر النشط «إدارة الفواتير»، والعنوان داخل الصفحة «فواتير المبيعات». الطيّ/التوسيع على سطح المكتب ودرج الجوال (`القائمة`) يعملان كما كانا. لا شريط تنقل سفلي.

---

## 7. استجابة / اتجاه / ثيم / حالات

تحقق عبر Chromium حقيقي ضد `http://127.0.0.1:3000` بعد دخول تجريبي (الوكيل البصري للمتصفح غير متاح في هذه البيئة؛ Playwright هو البديل الأقرب).

| السطح | النتيجة |
| --- | --- |
| سطح مكتب 1440 AR فاتح | قائمة كثيفة بلا فيض أفقي (`scrollWidth === clientWidth`) |
| معاينة سطح مكتب | صف نشط، فتح الفاتورة فقط للمرحّل، تعديل للمسودة، بلا قبض/ترحيل/مرتجع/طباعة/ZATCA |
| لا نتائج | «لا نتائج مطابقة» + مسح الفلاتر، لا «أنشئ أول فاتورة» |
| شريط مطوي | سكة أيقونات قائمة؛ المساحة بقيت مساحة الفواتير |
| داكن AR | التوكنز الداكنة؛ التأخير بلون التحذير مع النص |
| إنجليزي LTR | العنوان Sales Invoices؛ الشريط على اليسار؛ أسماء العملاء بيانات عربية |
| جهاز لوحي 768 | قائمة جدول مع تمرير أفقي داخل الحاوية؛ قائمة الجوال في الرأس |
| جوال 390 فاتح/داكن | بطاقات `MobileRecord`؛ الإجمالي أوضح من المتبقي؛ لا فيض |
| درج الجوال | الشريط القائم من جهة البداية فوق المحتوى |
| معاينة الجوال | لوحة كاملة العرض؛ فتح الفاتورة فقط للمرحّل المتأخر |
| تكبير 125% / 150% | التخطيط يبقى RTL داخل مساحة العمل |

Escape يغلق المعاينة. `/` يركّز البحث. لا شريط مؤشرات.

---

## 8. الاختبارات والبناء

### موجّه

```bash
cd web && npx vitest run \
  src/lib/invoices/workspace.test.ts \
  src/components/invoices/invoice-preview-panel.test.tsx \
  src/app/\(app\)/invoices/page.test.tsx \
  src/components/data-table.test.tsx
```

**46 passed** في هذه الملفات.

### كامل الواجهة

```bash
cd web && npm test
```

```
Test Files  205 passed (205)
Tests       1284 passed (1284)
Duration    22.24s
```

### البناء (Web CI يفرضه)

```bash
cd web && npm run build
```

نجح: Compile + Lint + TypeScript + 144 صفحة. لا اختبارات PHP — لا تغيير خادم.

---

## 9. اللقطات (من التطبيق التشغيلي)

لقطات Playwright ضد الخادم المحلي بعد «دخول تجريبي»، ليست نماذج تصميم.

| الملف | المحتوى |
| --- | --- |
| `desktop-ar-light.png` | قائمة عربية فاتحة |
| `desktop-ar-preview.png` | معاينة مرحّلة متأخرة — فتح الفاتورة فقط |
| `desktop-ar-no-results.png` | بحث بلا نتائج |
| `desktop-ar-sidebar-collapsed.png` | الشريط مطوي |
| `desktop-ar-dark.png` | قائمة داكنة |
| `desktop-ar-dark-preview.png` | معاينة داكنة لفاتورة مدفوعة |
| `desktop-en-ltr.png` | إنجليزي LTR |
| `tablet-ar.png` | لوحي |
| `mobile-ar-light.png` | جوال فاتح |
| `mobile-ar-dark.png` | جوال داكن |
| `mobile-ar-preview.png` | معاينة جوال |
| `mobile-ar-nav-drawer.png` | درج التنقل القائم |
| `desktop-ar-zoom-125.png` / `150.png` | تكبير |

---

## 10. المخاطر والحدود

- خصائص `DataTable` الجديدة اختيارية؛ الشاشات التي لا تمرّرها تبقى على السلوك السابق (تحرسه اختبارات الجدول).
- فرز/تصفية الديمو محلية في `mock-data.ts` لمسار القائمة؛ لوحة التحكم ما زالت تطلب `/invoices` بلا `per_page` فتعيد القائمة كاملة.
- المعاينة تطلب `GET /invoices/:id`؛ إن غابت `lines` تُعرض رسالة بلا سطور بدل الانهيار.
- تمييز التأخير بصري فقط وليس فلتر API.
- تكبير 125/150% محاكى بـ `document.body.style.zoom` في أداة اللقطات، لا عبر إعدادات المتصفح الأصلية.

---

## 11. أصغر خطوة تالية

إبقاء هذا طيارًا على `/invoices` حتى مراجعة الكثافة والمعاينة الخفيفة. لا تعميم الشريط أو اللوحة على المشتريات/المدفوعات قبل موافقة صريحة. لا عمل backend لمسار ملخص أو حالة «متأخرة» في العقد.
