<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required starting API 33 (Tiramisu) to show any notification at
         all; requested at runtime via the `awj/push` channel
         (MOBILE-RUNTIME-8, horizon MR-09) — see `MainActivity.kt`'s own
         doc comment. Declaring this permission is not itself a push
         transport commitment: it only gates whether this app may display a
         notification, independent of what eventually delivers one. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:label="awj_mobile_runtime"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Android App Link (MOBILE-RUNTIME-7, horizon MR-08). The host
                 is a placeholder under the IANA-reserved `.example` TLD (see
                 `mobile/lib/deeplink/deep_link_resolver.dart`'s own doc
                 comment) — no canonical AWJ mobile deep-link domain is
                 documented anywhere yet, so `autoVerify` cannot actually
                 succeed until a real domain hosts the matching
                 `.well-known/assetlinks.json`. That is an explicit open
                 Decision Gate, not silently assumed resolved; the
                 Dart-side validation this filter feeds into
                 (`DeepLinkController`/`resolveDeepLinkUri`) is what this
                 task actually proves. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="awj-runtime-proof.example"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
