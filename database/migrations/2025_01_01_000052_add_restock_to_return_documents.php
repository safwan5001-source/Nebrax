<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * ═══════════════════════════════════════════════════════════════
     *  هل تعود البضاعة المردودة إلى المخزون القابل للبيع؟
     * ═══════════════════════════════════════════════════════════════
     *  كان الجواب **«نعم دائماً»** مفروضاً: `ReturnService::postSalesReturn`
     *  يستدعي `applyReceipt` لكل صنفٍ متابَع بلا سؤال.
     *
     *  وهذا خطأ لنشاطٍ كامل: مطعمٌ أو محلّ أغذية لا يُعيد مردوداً إلى الرفّ
     *  أبداً، وتاجر تجزئةٍ يُعيد السليم ويُتلف التالف. **كلا السلوكين صحيح
     *  لصاحبه** — سياسةُ عملٍ لا صحّةٌ تقنية (انظر `design-system/foundations/
     *  configurable-policies.md`).
     *
     *  ــ لماذا هذا أخطر ما في القرارات الخمسة
     *
     *  إعادةُ التالف إلى المخزون تُدخِله بتكلفةٍ وكميةٍ لا وجود لهما على الرفّ،
     *  **فيُفسَد المتوسط المتحرك** — وكل تكلفة بضاعة مباعة لاحقة تخرج خاطئة،
     *  وينكسر الثابت `1140 = Σ(الكمية × متوسط التكلفة)`. أثرٌ يتعدّى المستند
     *  إلى كل ما يليه، تماماً كأثر المخزون السالب في #176.
     *
     *  ــ الحقل ثلاثي الحالة عمداً
     *
     *  `null` = «اتبع سياسة المستأجر» — تبقى المسوّدة تابعةً للسياسة حتى تُرحَّل
     *  ولو تغيّرت السياسة بينهما. وعند الترحيل تُحسَم القيمة وتُخزَّن، فيصير
     *  المستند المرحَّل **شارحاً لنفسه** لا مرهوناً بإعدادٍ قد يتغيّر بعده.
     *
     *  ــ لا يتغيّر شيء على القائم
     *
     *  العمود nullable، وافتراض السياسة `true` = سلوك اليوم حرفياً. ولا يمسّ
     *  مرتجعَ المشتريات أصلاً: الردّ إلى المورّد إخراجٌ من المخزون في كل حال،
     *  فلا سؤال فيه.
     */
    public function up(): void
    {
        Schema::table('return_documents', function (Blueprint $table) {
            $table->boolean('restock')->nullable()->after('payment_type');
        });
    }

    public function down(): void
    {
        Schema::table('return_documents', function (Blueprint $table) {
            $table->dropColumn('restock');
        });
    }
};
