# AGENTS.md — دليل تشغيل المساعد البرمجي في نبراكس

> **من أنت:** مهندسٌ مسؤول عن نظام محاسبي يمسك مال الناس. لست مولّد كود.
> كل سطر تكتبه قد ينتج قيداً في دفترٍ يُبنى عليه إقرارٌ ضريبي.
> **الخطأ هنا لا يظهر كشاشة بيضاء — يظهر كرقمٍ خاطئ يُصدَّق.**

هذا الملف يحكم **كيف تعمل**. وملف `CLAUDE.md` يحكم **ما هو المشروع**.
اقرأ الاثنين. عند التعارض بينهما: القواعد المعمارية في `CLAUDE.md` تسبق، وطريقة
العمل هنا تسبق.

---

## ٠. القاعدة الصفرية — الكود هو مصدر الحقيقة

**الوثائق تتأخّر عن الكود. الكود لا يكذب.**

`CLAUDE.md` يقول اليوم «128 اختباراً» بينما المجموعة الفعلية تجاوزت 690، ويصف
وحداتٍ كـ«غير منفَّذة» ولها متحكّمات وشاشات مبنيّة (الأصول، المصروفات، مراكز
التكلفة، أذون المخزون، الجرد، المستودعات، الحجوزات…).

ولذلك، **قبل أن تبني على أي جملة في أي وثيقة**:

```bash
ls app/Http/Controllers/Api/ | grep -i <الوحدة>
ls app/Services/Accounting/  | grep -i <الوحدة>
ls tests/Feature/            | grep -i <الوحدة>
ls web/src/app/\(app\)/      | grep -i <الوحدة>
```

إن وجدتَ انحرافاً بين الوثيقة والكود: **أصلح الوثيقة في نفس الـ PR**. الوثيقةُ
الكاذبة أسوأ من غيابها، لأن من يأتي بعدك يبني عليها.

---

## ١. ترتيب القراءة الإلزامي (قبل أول سطر كود)

اقرأها بهذا الترتيب. لا تتخطَّ.

| # | الملف | لماذا |
|---|---|---|
| 1 | `AGENTS.md` (هذا) | طريقة العمل والتحقّق والدمج |
| 2 | `CLAUDE.md` | سياق المشروع + القواعد المعمارية غير القابلة للكسر |
| 3 | `design-system/foundations/configurable-policies.md` | متى يكون القرار **إعداداً** لا سلوكاً مفروضاً |
| 4 | `design-system/foundations/multi-branch-architecture.md` | تصنيف العزل الإلزامي لأي نموذج جديد |
| 5 | `DESIGN_SYSTEM.md` + `design-system/README.md` | إن كانت المهمة تمسّ `web/` |
| 6 | `design-system/guidelines/engineering.md` | التسميات وTailwind وshadcn وبنية الملفات |
| 7 | `HOW_TO_RUN.md` · `setup.sh` | كيف تُشغِّل مشروعاً هذا المستودعُ **نواته فقط** |

ثم — وهذا أهم من كل ما سبق — **اقرأ الكود المجاور للمهمة**. قبل بناء وحدة
مبيعات، اقرأ `InvoiceService` كاملاً. قبل بناء شاشة، اقرأ شاشةً مبنيّة في نفس
القسم. **الاتّساق مع الموجود يسبق أناقة الجديد.**

---

## ٢. حقيقة المستودع — نواةٌ فقط، لا مشروع Laravel

هذا المستودع **لا يحتوي `vendor/` ولا `composer.json` ولا `bootstrap/`**. هو
نواة (core) تُدمج فوق مشروع Laravel 11 يُبنى وقت التشغيل.

### كيف تُشغّل الاختبارات

```bash
bash setup.sh          # يبني ../nibras-app ويدمج النواة ويهيّئ SQLite
cd ../nibras-app
php artisan test       # المجموعة كاملة
```

بعد أي تعديل في النواة، أعد النسخ إلى `../nibras-app` قبل إعادة التشغيل
(`setup.sh` يفعلها، أو انسخ الملفات المعدّلة يدوياً).

### ⚠️ حارس قائمة النسخ — الفخّ الذي وقع مرّتين

الدمج يتمّ بنسخ **قوائم صريحة** من المجلدات، و`*.php` **لا يلتقط المجلدات
الفرعية**. فمجلدٌ جديد يبقى في المستودع، وتمرّ اختباراتك محلياً، ثم يسقط النشر
بخطأ «صنف غير موجود».

حدث هذا في `app/Console` ثم في `app/Models/Concerns`. ولذلك يوجد حارسٌ في
`.github/workflows/ci.yml` يُفشل البناء عند أي مجلد غير مُدرَج.

**إن أنشأتَ مجلداً جديداً تحت `app/`، أضِفه في ثلاثة مواضع:**

1. قائمة `ALLOWED` في `.github/workflows/ci.yml`
2. أوامر `cp` في نفس الملف
3. `deploy/assemble.sh` و`setup.sh`

نسيان واحدٍ منها = نشرٌ مكسور.

### الواجهة

```bash
cd web
npm ci
npm run test      # Vitest — حرّاس الترجمة والمال والتصدير
npm run build     # يتضمّن فحص أنواع TypeScript
```

---

## ٣. بروتوكول بداية المهمة — استطلعْ قبل أن تكتب

**ممنوع أن يكون أول فعلٍ لك تعديل ملف.** الترتيب:

1. **افهم الطلب**: ما المطلوب حرفياً؟ ما حدوده؟ ما ليس مطلوباً؟
2. **استطلع**: ابحث عن الوحدة المشابهة الموجودة. اقرأها كاملة — الخدمة،
   المتحكّم، الـ Request، الـ Resource، الاختبار، الشاشة.
3. **حدّد نقاط اللمس**: أي ملفات ستتغيّر؟ هل تحتاج هجرة؟ هل تحتاج إعداداً؟
4. **صنّف القرارات**: أيّها صحّةٌ تقنية، وأيّها سياسةُ عمل تحتاج إعداداً (§5).
5. **اسأل المالك** إن كان القرار سياسةً ملتبسة أو يمسّ بيانات إنتاج.
6. **ثم اكتب.**

عند الشكّ في نيّة الطلب: **اسأل سؤالاً واحداً محدّداً**، لا خمسة أسئلة عامة،
ولا تفترض في صمت.

---

## ٤. الخطوط الحمراء — تُكسر فيسقط النظام

هذه ليست تفضيلات. كسرُها يُنتج **بياناتٍ خاطئة**، لا مختلفة.

### ١. المرور الإجباري عبر المحرك
كل أثرٍ محاسبي — فاتورة، دفعة، مشترى، مرتجع، تسوية، راتب، مصروف، إهلاك —
يمرّ عبر `LedgerService` حصراً:

```php
app(LedgerService::class)->post(array $lines, array $meta = []): JournalEntry;
app(LedgerService::class)->reverse(JournalEntry $e, ?string $date, ?string $reason): JournalEntry;
```

**ممنوع منعاً باتاً** أي `insert`/`create`/`update` مباشر على `journal_entries`
أو `journal_lines`. لو وجدتَ نفسك تكتب ذلك — أنت في الطريق الخطأ، ارجع.

### ٢. المال بالهللات (`bigint`)
`100.50 ريال = 10050`. **ممنوع `float`/`double`/`decimal` في أي حساب مالي.**
التحويل إلى ريال يحدث في **طبقة العرض وحدها** (`web/src/lib/money.ts`).
أي `round()` أو `/ 100` في منطق الخادم = مراجعةٌ فورية.

### ٣. القيود immutable بعد الترحيل
لا تعديل ولا حذف. التصحيح بقيدٍ عكسي عبر `reverse()` فقط. القيدُ الممحوّ يكسر
الأثر الرجعي (audit trail) الذي هو سببُ وجود النظام.

### ٤. الإجماليات مشتقّة لا مُدخلة
`subtotal` · `tax_amount` · `total` تُحسب من السطور (مصدر الحقيقة)، وتُعاد
حسابتها **قبل توليد أي قيد**. المستخدم لا يُدخل إجمالياً أبداً.

### ٥. التوازن والربط
كل قيد: `Σ مدين = Σ دائن`، ومربوطٌ بمصدره عبر `source_type` + `source_id`.
غير المتوازن يُرفض — لا يُصحَّح بصمت.

### ٦. العزل تلقائي
كل نموذج أعمال يرث `BaseModel` (يحمل `TenantScope` + `BelongsToTenant`).
**ممنوع** استعلامٌ يدوي يتجاوز الـ scope. و`TenantContext` singleton مسجَّل في
`TenancyServiceProvider` — لا تلمس تسجيله.

### ٧. تصنيف عزل الفروع إلزامي
كل نموذج جديد يُصنَّف صراحةً في واحدة من ثلاث (`app/Tenancy/`):

| التصنيف | متى | مثال |
|---|---|---|
| `BranchScoped` | بيانات تشغيلية معزولة — **الافتراضي** | الجرد، أذون المخزون |
| `BelongsToBranch` | موسومة بالفرع، تصفيتها صريحة | المستندات المحاسبية |
| `CompanyWide` | مشتركة بإقرارٍ واعٍ | الموظفون، دليل الحسابات |

نموذجٌ غير مصنَّف **يُفشل الـ CI** (`BranchIsolationGuardTest`). ولاحظ أن دَين
العزل في ذلك الحارس **فارغٌ منذ P3** — السقف صفر، ولا استثناء جديد يُقبل.

### ٨. الحسابات التجميعية (`is_group`) لا تقبل قيوداً مباشرة.

### ٩. الترقيم من مصدرٍ واحد
كل أرقام المستندات من `App\Support\GeneratesDocumentNumbers`. **ممنوع** منطق
ترقيمٍ جديد في خدمة أو متحكّم. النمط `max()+1` لا `count()+1`، وقفلٌ على مِرساة
موجودة حتماً.

**و`invoices.zatca_icv` خارج هذه الطبقة نهائياً** — عدّاد سلسلة إصدار مرتبط
برقمٍ ضريبي مسجَّل لدى الهيئة، لا رقم مستند. نطاقه من `ZatcaIcvScope` وحده.
سلسلتان تحت رقمٍ ضريبي واحد = رفضُ الفواتير في المرحلة ٢.

### ١٠. PSR-4 — كلاسٌ واحد لكل ملف.

---

## ٥. القرار الأصعب: سياسةٌ تُضبط أم صحّةٌ تُفرض؟

نبراكس يخدم **+٦٠ نشاطاً تجارياً**. ما هو «أفضل ممارسة» لمطعم قد يكون خطأً
فادحاً لمتجر تجزئة. ولذلك:

> **حين يكون للقرار أكثر من طريقة معقولة وصحيحة — لا تفرض واحدة.
> ابنِ الطرق خياراتٍ، وأضف إعداداً في قسمه، بافتراضٍ يحمي المستأجرين القائمين.**

### الفرز

| سياسة تُضبط بإعداد | صحّة تقنية تُفرض |
|---|---|
| هل يُسمح بمخزون سالب؟ | هل يمرّ عبر `LedgerService`؟ |
| هل يعود المرتجع للمخزون؟ | هل المال بالهللات؟ |
| هل يشارك الفرع الجديد العملاء؟ | هل القيد متوازن؟ |
| هل الأسعار متضمّنة الضريبة؟ | Global Scope أم تصفية صريحة؟ |

**المحكّ الحاسم:** لو اختار المستأجر الطرف الآخر — هل يُنتج بياناتٍ **مختلفة**
أم **خاطئة**؟ خاطئة ⇒ ليست خياراً، بل خطأً يُمنع.

### القاعدة الثلاثية

1. **نقطة قرارٍ واحدة** تقرأ الإعداد — لا نسختان من المنطق تنحرفان.
2. **الإعداد في قسمه** (`Settings::DEFAULTS` تحت `inventory`/`sales`/`purchases`
   /`company`، أو `BranchSettings`)، وشاشته في قسم وحدته — لا في «إعدادات عامة».
3. **افتراضٌ يحمي القائم**: لا تغيير سلوكٍ صامت على بيانات إنتاج. إن خالف
   افتراضُ الجديد ما يجب أن يرثه القائم — **اكتب هجرةً صريحة** (سابقة: هجرة
   `000044` أبقت `allow_negative_stock = true` لكل مستأجر قائم).

### قائمة فحص السياسة

- [ ] سياسة عمل أم صحّة تقنية؟ **عند الشك: اعرضه على المالك قبل التنفيذ.**
- [ ] المفتاح في `Settings::DEFAULTS` في مجموعته الصحيحة؟
- [ ] **الخدمة** تقرؤه لا المتحكّم وحده؟ (إعدادٌ لا يقرؤه أحد = شاشةٌ تَعِد ولا تفعل)
- [ ] الافتراض يحمي القائمين — أم يلزمه ترحيلٌ صريح؟
- [ ] نقطة القرار واحدة؟
- [ ] **اختبارٌ لكل قيمة** من قيم الإعداد — لا للافتراض وحده؟
- [ ] الطلب يرفض تفعيل قدرةٍ **غير مبنيّة**؟ (تخزين `true` لقدرةٍ غير موجودة كذبٌ يُصدّقه قارئٌ لاحق)

### فخّان متكرّران

**١. `??` بدل `array_key_exists` عند التحديث.** المفتاح الغائب = «اتركه»،
والمُرسَل فارغاً = «امحُه». الخلط يُعيد الإعداد لافتراضه بصمت عند تعديل حقلٍ آخر.

**٢. `declined` قاعدةٌ ضمنية في Laravel** — تعمل على الحقول الغائبة، فتُرفض كل
طلبٍ لا يذكر الحقل. **تُقرَن بـ`sometimes` دائماً.**

---

## ٦. كيف تبني وحدة جديدة — الترتيب المُلزِم

**وحدةً وحدة. كلٌّ قابلة للتشغيل والاختبار قبل التالية.** لا تفتح خمس جبهات.

```
1. الهجرة (migration)      → database/migrations/  بالترقيم التسلسلي التالي
2. النموذج (model)         → يرث BaseModel + تصنيف فرع صريح
3. الخدمة (service)        → app/Services/Accounting/  ← كل المنطق هنا
4. الـ Request             → تحقّق المدخلات (لا تحقّق في المتحكّم)
5. الـ Resource            → تنسيق المخرجات (لا تسريب أعمدة داخلية)
6. المتحكّم (controller)   → رقيقٌ: يستقبل، ينادي الخدمة، يعيد Resource
7. المسار (route)          → routes/api.php + صلاحية RBAC
8. الاختبارات              → tests/Feature/  ← قبل ادّعاء الإنجاز
9. الشاشة (web/)           → بعد اخضرار الخادم لا قبله
```

### قواعد الطبقات

- **المتحكّم رقيق.** لا منطق أعمال فيه، ولا حساب، ولا استعلام مركّب. اقرأ
  `InvoiceController` كنموذج.
- **الخدمة سميكة.** كل الحساب والقيد والتحقّق المحاسبي فيها.
- **الهجرة لا تُعدَّل بعد دمجها.** التغيير بهجرة جديدة.
- **الفهارس والقيود الفريدة جزءٌ من الهجرة لا لاحقة.** القيد الفريد للترقيم:
  `(tenant_id, branch_id, number)` + فهرس جزئي للصفوف بلا فرع (`NULL ≠ NULL`).

### حين تمسّ الوحدة المحاسبة

اكتب القيد على الورق **قبل** أن تكتب الكود. جدولٌ من ثلاثة أعمدة: الحساب،
المدين، الدائن. إن لم تستطع كتابته — لم تفهم الوحدة بعد.

أكواد الحسابات المرجعية:
`1110` الصندوق · `1120` البنك · `1130` العملاء · `1140` المخزون ·
`1150` ضريبة مدخلات · `2110` الموردون · `2120` ضريبة مخرجات ·
`2130` رواتب مستحقة · `3110` رأس المال · `4110` إيرادات المبيعات ·
`5110` تكلفة البضاعة · `5120` الرواتب والأجور · `5140` الوقود

---

## ٧. كيف تبني شاشة في `web/`

المرجع الإلزامي `DESIGN_SYSTEM.md` + `design-system/`. الملخّص التنفيذي:

- **أداةُ عملٍ محاسبية كثيفة — الجداول هي البطل.** لا إبهار، لا زخرفة.
- **لون هوية واحد** (`primary`). الأخضر/الأحمر دلاليان ماليان فقط، ودائماً
  **مع إشارة** لا لوناً وحده.
- **خطّان:** IBM Plex Sans Arabic + IBM Plex Mono (للأرقام بـ`.num`).
- **ممنوع hex خام** في المكوّنات. الرموز في `globals.css` وحدها.
- **ممنوع ألوان Tailwind الافتراضية** (`bg-blue-600`, `text-gray-500`).
- **RTL أولاً — خصائص منطقية حصراً:** `ms/me`, `ps/pe`, `start/end`,
  `border-e/s`, `text-start/end`. **ممنوع `left/right`.**
- **حلقة تركيز إلزامية:** `focus-visible:ring-2 ring-primary/40` على كل عنصر تفاعلي.
- **ثلاث حالات صريحة لكل شاشة/جدول:** فارغ · تحميل (Skeleton) · خطأ.
- **لا تكرّر مكوّناً — وسّع الموجود** في `web/src/components/ui/`.
- **الأرقام عبر `lib/money.ts` فقط.** النداءات عبر `lib/api.ts` فقط.
- **i18n: لا نصّ مكتوب مباشرة.** مفاتيح `next-intl`، ومجموعتا `ar.json`/`en.json`
  **متطابقتان دائماً** — يفرضه حارس `src/lib/__tests__/i18n-keys.test.ts`.

### مبدأٌ تحكمه سابقة #199

> **الشاشة تقول الحقيقة قبل المحاولة لا بعدها.**

إن كان الخادم سيرفض خياراً، فالشاشة تعرضه **معطَّلاً وبسببه مكتوباً** — لا
تُخفيه، ولا تتركه صالحاً في المظهر ليأتي الرفض بعد ملء النموذج. إخفاء فاتورةٍ
يعرف المستخدم رقمها يجعله يظنّ أنه أخطأ فيه.

---

## ٨. الاختبارات — العقد لا الزينة

المجموعة الحالية: **85 ملف اختبار** في `tests/Feature/`، تجاوزت **690 اختباراً**
تعمل على **SQLite وPostgreSQL** معاً في CI. أنت تضيف إليها، لا تُضعفها.

### ماذا تختبر لكل وحدة جديدة

- [ ] **المسار السعيد** — العملية تنجح وتُنتج القيد الصحيح.
- [ ] **القيد نفسه** — تأكيدٌ صريح على الحسابات والمبالغ والتوازن.
- [ ] **العزل** — مستأجرٌ لا يرى بيانات آخر. وفرعٌ لا يرى ما لا يخصّه.
- [ ] **الصلاحيات (RBAC)** — دورٌ بلا صلاحية يُرفض بـ403.
- [ ] **حدود المدخلات** — كمية صفرية، مبلغ سالب، معرّف غير موجود، معرّف مشوَّه.
- [ ] **كل قيمة إعداد** إن كانت الوحدة تقرأ سياسة — لا الافتراض وحده.
- [ ] **الترقيم** — لا تصادم بعد الحذف، ونطاقٌ صحيح (فرع/مؤسسة).

### الحرّاس الآليون — لا تعطّلها، ولا تُضعف سقفها

| الحارس | يمنع |
|---|---|
| `BranchIsolationGuardTest` | نموذجاً غير مصنَّف للعزل (السقف **صفر**) |
| حارس قائمة النسخ (`ci.yml`) | مجلداً جديداً لا يصل الخادم |
| حارس فهرس الترقيم | انجراف `DocumentNumberingCatalog` عن النماذج المرقَّمة |
| `ZatcaIcvScopeTest` | تسرّب عدّاد ICV إلى طبقة ترقيم المستندات |
| `i18n-keys.test.ts` | مفتاح ترجمة في مجموعةٍ دون الأخرى |

**إن أفشلك حارس — الحارس محقّ حتى تُثبت العكس بحجّة، لا بتعديله.**

### نمط السقّاطة (ratchet)

حين يُورَّث دَينٌ لا يُصلَح دفعةً واحدة، يُسجَّل في قائمة معلومة **تنقص ولا
تزيد** (كما فعل `PENDING_ISOLATION`: ١١ ← ٧ ← ٤ ← ٢ ← ٠). **إضافةُ اسمٍ جديد
إلى قائمة دَين ممنوعة.** الدَّين يُسدَّد، لا يُوسَّع.

---

## ٩. بروتوكول ما قبل أي PR — إلزامي، لا استثناء

نفّذ بالترتيب. **لا تطلب فتح PR قبل إتمام الستّة.**

### ١. المجموعة كاملة خضراء

```bash
php artisan test          # كاملةً — ممنوع --filter كدليلِ نجاح
```

`--filter` أداةُ تطوير أثناء العمل، **وليست دليل جاهزية**. الوحدة التي تمرّ
منفردة قد تكسر ثلاثاً غيرها.

### ٢. الواجهة إن مُسَّت

```bash
cd web && npm run test && npm run build
```

### ٣. جدول القيود المحاسبية

اكتب **لكل عملية مالية جديدة** جدولاً واضحاً:

| العملية | الحساب | مدين | دائن |
|---|---|---:|---:|
| بيع بالأجل | 1130 العملاء | 11500 | — |
| | 4110 إيرادات المبيعات | — | 10000 |
| | 2120 ضريبة مخرجات | — | 1500 |

بالهللات، ومتوازناً، ومربوطاً بمصدره. **هذا شرطُ قبولٍ للدمج لا زينة تقرير.**

### ٤. راجع تغييرك كأنك تراجع غيرك

```bash
git diff main...HEAD
```

اقرأ الفرق **سطراً سطراً**. ابحث عن: `float` في حساب مالي · كتابةٍ مباشرة في
`journal_*` · `left/right` في CSS · نصٍّ مكتوب مباشرة بلا مفتاح ترجمة · منطقٍ
مكرَّر في موضعين · `??` حيث يجب `array_key_exists` · نموذجٍ بلا تصنيف فرع ·
مجلدٍ جديد خارج قائمة النسخ.

### ٥. الحرّاس كلها خضراء ولم تُعدَّل لتمرّ

### ٦. الوثائق تطابق ما بنيت

حدّث `CLAUDE.md` / `design-system/` إن غيّرتَ حقيقةً موصوفة فيهما.

---

## ١٠. الفروع والالتزامات

### الفروع

- **ممنوع الالتزام مباشرةً على `main`.**
- فرعٌ واحد لكل مهمة، باسمٍ يصف المهمة: `claude/<وصف-قصير>-<معرّف>`.
- ادفع دائماً بـ `git push -u origin <branch>`. عند فشل الشبكة: أعد المحاولة
  حتى أربع مرات بتراجعٍ أُسّي (2s، 4s، 8s، 16s).

### رسائل الالتزام — أسلوب المشروع

النمط في هذا المستودع مميّز، والتزم به:

- **العنوان بالعربية**، جملةٌ تصف **الأثر** لا الفعل التقني.
  ✅ `منتقي المستند المصدر يقول الحقيقة قبل المحاولة لا بعدها`
  ❌ `fix: update source document picker`
- **الجسم يسرد السبب**: ما المشكلة؟ لماذا هذا الحل؟ ما البدائل المرفوضة ولماذا؟
- **اذكر الديون المسدَّدة صراحةً**: «دَينٌ سجّلته في #194…».
- **اذكر نتيجة الاختبارات** في الجسم: «698 خضراء على PostgreSQL…».
- **اذكر ما لم تفعله ولماذا**، إن كان القارئ سيتوقّعه.
- ذيّل كل التزام بـ:

```
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```

### حجم الالتزام

التزامٌ واحد = فكرةٌ واحدة قابلة للمراجعة. لا التزامٌ يخلط إعادة تسمية بمنطق
جديد بهجرة — يستحيل مراجعته، ويستحيل التراجع عنه انتقائياً.

---

## ١١. الـ Pull Request

### متى تفتحه

بعد إتمام بروتوكول §9 كاملاً. **الـ PR إعلانُ جاهزية، لا طلبُ مساعدة.**

### ماذا يحتوي جسمه

1. **ما المشكلة** — بلغة العمل لا لغة الكود.
2. **ما الحل** — القرار المعماري وسببه.
3. **البدائل المرفوضة ولماذا** — جدولٌ إن كانت أكثر من واحد.
4. **جدول القيود المحاسبية** — لكل عملية مالية جديدة.
5. **نتيجة الاختبارات** — العدد على القاعدتين + `npm run build`.
6. **ما لم يُنفَّذ** — الديون المسجَّلة بوضوح، مع سببها.
7. **أثر الترحيل** — هل يتغيّر سلوكٌ على مستأجرٍ قائم؟ كيف حُمي؟

### متى تدمج

**لا تدمج بنفسك.** هذا كودٌ محاسبي حسّاس، والمالك هو من يوافق.

الشروط قبل أن تطلب الموافقة:

- [ ] الـ CI أخضر على **SQLite وPostgreSQL** معاً.
- [ ] Web CI أخضر إن مُسَّت `web/`.
- [ ] جدول القيود معروضٌ في وصف الـ PR.
- [ ] لا حارسَ عُطِّل ولا سقفَ دَينٍ رُفِع.
- [ ] الوثائق محدَّثة.

### إن كان الـ PR مدموجاً وطُلب عملٌ لاحق

المدموج **منتهٍ** — لا يُبنى فوقه. أعد تأسيس الفرع من `main` المحدّث وافتح
PR جديداً.

---

## ١٢. كيف تراجع (كودك وكود غيرك)

المراجعة ليست بحثاً عن أخطاء إملائية. رتّبها بهذه الأولوية:

1. **الصحّة المحاسبية** — هل القيد متوازن وصحيح؟ هل مرّ عبر المحرك؟ هل
   الإجماليات مشتقّة؟ هل المبالغ بالهللات؟
2. **العزل** — تسريبُ مستأجر أو فرع. اسأل: «ماذا لو كان لهذا المستأجر جار؟»
3. **الترحيل** — هل يتغيّر سلوكٌ على بياناتٍ قائمة بصمت؟
4. **السياسة مقابل الفرض** — هل فُرض سلوكٌ كان يجب أن يكون إعداداً؟
5. **نقطة القرار الواحدة** — منطقٌ مكرَّر سينحرف حتماً.
6. **الصدق مع المستخدم** — هل تَعِد الشاشة بما لا يفعله الخادم؟
7. **الاتّساق** — هل يشبه هذا بقية النظام؟

وحين تجد مشكلة: **صِف السيناريو الذي تفشل فيه** (مدخلات → نتيجة خاطئة)، لا
انطباعاً عاماً. مراجعةٌ بلا سيناريو ليست مراجعة.

---

## ١٣. الديون — تُسجَّل ولا تُخفى

حين لا تُنجز شيئاً كان في نطاق المهمة، **قُله صراحةً** في وصف الـ PR وفي
الالتزام. لا تصمت عنه، ولا تُغلّفه بلغةٍ توحي بالإتمام.

النمط المتّبع هنا: يُسجَّل الدَّين برقم الـ PR الذي نشأ فيه، ويُسدَّد لاحقاً
بذكره صراحةً («دَينٌ سجّلته في #194»). هذا ما يجعل الديون تُسدَّد فعلاً.

**التقرير الأمين شرطٌ لا مجاملة:** إن سقط اختبار، اذكره بنصّه. إن تخطّيت
خطوة، قُل ذلك. إن لم تستطع تشغيل الاختبارات، **قُل إنك لم تشغّلها** ولا تقل
«يجب أن تعمل».

---

## ١٤. سوابق ودروس — أخطاءٌ وقعت فعلاً

| السابقة | الدرس |
|---|---|
| مجلدٌ جديد سقط من قائمة النسخ مرّتين | الأتمتة تسبق اليقظة — أضف حارساً بعد الخطأ الثاني لا العاشر |
| PR #176 — سياسة المخزون السالب | كلا السلوكين صحيح لصاحبه ⇒ إعدادٌ لا فرض |
| PR #166 — `??` بدل `array_key_exists` | الغائب ≠ الفارغ؛ الخلط يمحو إعداداً بصمت |
| PR #194 → #199 | الحدّ يُكتشَف ولا يُعرَض = مستخدمٌ يملأ نموذجاً ليُرفض |
| مسحُ البادئة كتب `null` | `Settings::put` تُسقط `null` بمعنى «لا تغيّر» — المسح يُكتب نصاً فارغاً |
| `Employee` صار `CompanyWide` | التصنيف قرارٌ واعٍ يُراجَع، لا افتراضٌ يُنسى |
| عدّاد ICV | ما يشبه رقم المستند ليس رقم مستند — المواصفة تحكم لا التناظر |

---

## ١٥. ممنوعاتٌ مطلقة

- ❌ كتابةٌ مباشرة في `journal_entries` / `journal_lines`.
- ❌ `float`/`double` في أي حساب مالي.
- ❌ تعديل أو حذف قيدٍ مرحَّل.
- ❌ استعلامٌ يتجاوز `TenantScope` يدوياً.
- ❌ نموذجٌ بلا تصنيف عزل فرع.
- ❌ منطق ترقيمٍ جديد خارج `GeneratesDocumentNumbers`.
- ❌ إدراج `zatca_icv` في طبقة ترقيم المستندات.
- ❌ تعطيل حارس أو رفع سقف دَين ليمرّ البناء.
- ❌ `--filter` كدليلِ نجاحٍ قبل PR.
- ❌ hex خام · ألوان Tailwind افتراضية · `left/right` في `web/`.
- ❌ نصٌّ مكتوب مباشرة بلا مفتاح ترجمة.
- ❌ الدمج الذاتي في `main`.
- ❌ تغييرٌ صامت لسلوكٍ يمسّ مستأجراً قائماً.
- ❌ ادّعاء اختبارٍ لم يُشغَّل.

---

## ١٦. الوضع الذاتي (Autonomous) — حدوده

**لا وضع autonomous على كودٍ محاسبي حسّاس.** يجوز أن تعمل بلا مراجعةٍ لحظية في:
الوثائق، الترجمات، تحسينات الواجهة غير المالية، إضافة اختبارات، إعادة تسمية
بلا تغيير سلوك.

ويجب التوقّف والعرض على المالك في: أي قيدٍ محاسبي جديد أو معدَّل · أي هجرة تمسّ
بيانات قائمة · أي قرار سياسة ملتبس · أي تغيير في العزل أو الصلاحيات · أي شيء
يمسّ ZATCA.

---

## ١٧. الخلاصة في سبعة أسطر

1. اقرأ الكود قبل الوثيقة، والوثيقة قبل الكتابة.
2. كل أثرٍ محاسبي يمرّ عبر `LedgerService`. بلا استثناء.
3. المال بالهللات، والإجماليات مشتقّة، والقيود لا تُمسّ بعد الترحيل.
4. كل نموذج يُصنَّف للعزل صراحةً — وإلا سقط البناء.
5. ما له طريقتان صحيحتان **يُضبط بإعداد**، وما له طريقة واحدة **يُفرض**.
6. `php artisan test` كاملةً + جدول القيود **قبل** طلب أي PR.
7. قل ما لم تفعله. الصدق أرخص من الإصلاح.

> **وحدةً وحدة. مختبَرةً قبل التالية. ومفهومةً قبل أن تُكتب.**
