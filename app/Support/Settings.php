<?php

namespace App\Support;

use App\Models\Tenant;
use App\Tenancy\TenantContext;

/**
 * ═══════════════════════════════════════════════════════════════
 *  تفضيلات المستأجر — قراءة موحّدة من `tenants.settings[$group]`
 * ═══════════════════════════════════════════════════════════════
 *  مصدر واحد تقرأ منه **الخدماتُ** الإعدادات، لا المتحكّمات وحدها. بلا هذا
 *  المصدر يبقى الإعداد محفوظاً ولا يقرؤه أحد — شاشةٌ تَعِد ولا تفعل، وهو
 *  أسوأ من غياب الشاشة أصلاً.
 *
 *  التخزين في عمود `settings` من نوع json على `tenants` — لا جدول ولا هجرة.
 */
class Settings
{
    /** الافتراضات لكل مجموعة. المفتاح غير المعرَّف هنا لا يُقرأ ولا يُكتب. */
    public const DEFAULTS = [
        /**
         * هوية الشركة. `logo` صورة (data URL) تُعرَض في **قشرة التطبيق**
         * (الشريط العلوي والجانبي) وفي رؤوس المستندات.
         *
         * موضعها هنا لا في `sales_config.designs`: القشرة تظهر لكل مستخدم في
         * كل صفحة، ووضعُ الشعار خلف صلاحية `invoices.view` كان يُفقده لمن لا
         * يفتح فاتورة. انظر هجرة 000048.
         */
        'company' => [
            'logo'            => '',
            // بيانات النشاط النظامية. قيم فارغة افتراضياً حتى لا يتغيّر أي
            // مستند قائم؛ تُعرض فقط حين تُسجَّل في ملف المؤسسة.
            'phone'           => '',
            'mobile'          => '',
            'building_no'     => '',
            'street'          => '',
            'additional_no'   => '',
            'district'        => '',
            'city'            => '',
            'postal_code'     => '',
            'short_address'   => '',
        ],
        // الافتراضات هي **العقد المنشور سابقاً حرفياً** — لم يُغيَّر منها شيء
        // عند التوصيل. ولا سلوكَ تأثّر: كل مستدعٍ قائم يمرّر `payment_type`
        // صراحةً، فالاحتياطي القديم (`cash` في `InvoiceService`) لم يكن
        // يُبلَغ أصلاً — كان الحقل `required` في الطلب.
        //
        // `default_terms` يبقى في العقد ولا يقرؤه أحد: هو نسخة مكرّرة من
        // `terms_text` في إعدادات تصميم المستندات، وتلك **تُعرَض فعلاً** على
        // الفاتورة والعرض. وصلُه هنا كان سيطبع الشروط مرّتين، وحذفُه قرارٌ
        // يخصّ مالك المنتج لأن مستأجرين قد ملؤوه. انظر ملاحظة الـ PR.
        'sales' => [
            'default_tax_rate'     => 15,
            'default_payment_type' => 'credit',
            'quote_validity_days'  => 14,
            'invoice_prefix'       => 'INV',
            // بادئة عرض السعر — تُضبط من «إعدادات الترقيم المتسلسل» كأختها.
            // مستقلّة عن بادئة الفاتورة عمداً: العرض والفاتورة مستندان يخرجان
            // للعميل في مسار واحد، وقد يريد المستأجر تمييزهما أو توحيدهما.
            'quote_prefix'         => 'QUO',
            'default_terms'        => '',
            /**
             * هل يلزم مرتجعَ المبيعات فاتورةٌ مصدر؟
             *
             * **سياسة عمل لا صحّة تقنية** (انظر `design-system/foundations/
             * configurable-policies.md`): التجزئة تريد «لا ردّ بلا إيصال»،
             * والمقاولات والخدمات تُصدر إشعارات دائنة مستقلّة لتسويةٍ
             * متفاوَض عليها. كلاهما يُنتج بياناتٍ صحيحة.
             *
             * **الافتراض `false`** — وهو سلوك اليوم حرفياً، فلا يتعطّل
             * مستأجرٌ قائم يُنشئ مرتجعات حرّة بترقية.
             */
            'require_return_source' => false,
            /**
             * مهلة الردّ بالأيام من تاريخ الفاتورة — **`0` = بلا حدّ**.
             *
             * **سياسة عمل لا صحّة تقنية:** التجزئة تضع مهلةً معلنة (١٤ يوماً)،
             * والمقاولات والعقود طويلة الأجل لا مهلة فيها أصلاً. كلاهما
             * يُنتج بياناتٍ صحيحة.
             *
             * وتُقاس **من تاريخ المستند المصدر**، فلا معنى لها بلا مصدر:
             * المرتجع الحرّ لا مرجع زمني له.
             */
            'return_window_days'    => 0,
            /**
             * هل يُرفض سعر بيع أقل من `products.min_sale_price`؟
             *
             * **سياسة مبيعات قابلة للضبط:** الافتراض مفعّل للمستأجر الجديد،
             * والاستثناء لا يمر إلا بصلاحية صريحة وسبب محفوظ على سطر الفاتورة.
             * أمّا المستأجرون القائمون فيثبّتهم ترحيل 000088 على `false` حتى
             * يفعّلوا السياسة بقرار واعٍ؛ فلا تتعطل فواتيرهم بترقية صامتة.
             */
            'enforce_min_sale_price' => true,
        ],
        /**
         * إعدادات المخزون والمنتجات.
         *
         * ── `allow_negative_stock` — هل يُسمح ببيع ما ليس في المخزن؟
         *
         * **الافتراض `false`** — رصيدٌ دائن على حساب أصل خطأ لا سياسة، وأثره
         * يتعدّى الرقم السالب: المتوسط المتحرك يُفسَد بعده فتصير كل تكلفة
         * بضاعة مباعة لاحقة خاطئة.
         *
         * لكن المستأجرين **القائمين** يُبقيهم الترحيل على `true` (هجرة 000044)
         * — فمن عنده مخزون سالب اليوم لا تتوقّف مبيعاته فجأةً بترقية.
         *
         * **يبقى ثنائياً عمداً وإن عُرض في الشاشة خيارَين.** دلالته الكاملة
         * «السماح للمتابَع **كمّياً** دون المتابَع تفصيلياً (رقم مسلسل/شحنة/
         * صلاحية)» — لأن رقماً مسلسلاً غير موجود لا يُباع أصلاً فلا معنى
         * للسالب فيه. والتتبع التفصيلي غير مبنيّ بعد، فالشقّ الثاني من الشرط
         * صادقٌ خاوٍ اليوم: `true` تعني حالياً «لكل متابَع كمّياً» لأن كل
         * المنتجات كذلك. حين يُبنى التتبع التفصيلي يكتسب الحقل دلالته كاملةً
         * بإضافة شرط واحد في `assertStockAvailable` — **بلا تغيير نوع ولا
         * هجرة بيانات ولا إعادة اختيار من المستأجر**.
         *
         * ── `detailed_tracking_enabled` — التتبع بالرقم المسلسل/الشحنة/الصلاحية.
         *
         * **للقراءة فقط في هذه المرحلة**: الحقل معرَّف ليقرأه العميل ويعرض
         * حالته، لكن `UpdateInventorySettingsRequest` يرفض تفعيله. تخزين
         * `true` لقدرةٍ غير مبنيّة كذبٌ يُصدّقه قارئٌ لاحق ويبني عليه.
         *
         * ── `show_stock_quantities` — عرض الكمية في شاشات المنتجات (عرض بحت).
         */
        'inventory' => [
            'allow_negative_stock'      => false,
            'detailed_tracking_enabled' => false,
            'show_stock_quantities'     => true,
            /**
             * ── `restock_sales_returns` — هل يعود مردود المبيعات للمخزون؟
             *
             * **سياسة عمل لا صحّة تقنية:** مطعمٌ أو محلّ أغذية لا يُعيد مردوداً
             * إلى الرفّ أبداً، وتاجر تجزئةٍ يُعيد السليم. كلاهما صحيح لصاحبه.
             *
             * **الافتراض `true`** — سلوك اليوم حرفياً، فلا يتغيّر شيء على
             * مستأجر قائم. ومن يُطفئه تُرحَّل تكلفةُ المردود إلى **5180 فروق
             * الجرد والتلف** بدل أن تعود إلى 1140.
             *
             * والمستند يعلو على السياسة: `return_documents.restock` يقبل
             * قيمةً صريحة لمرتجعٍ بعينه (بضاعة عادت تالفة في متجرٍ يُعيد
             * السليم عادةً)، و`null` تعني «اتبع هذه السياسة».
             *
             * لا يمسّ مرتجع المشتريات: الردّ إلى المورّد إخراجٌ في كل حال.
             */
            'restock_sales_returns'     => true,
        ],
        'purchases' => [
            'default_tax_rate'      => 15,
            'default_payment_type'  => 'credit',
            'default_tax_inclusive' => false,
            'purchase_prefix'       => 'BILL',
            // مثيله في `sales` — مفتاحان لا مفتاح: قد تُلزِم المؤسسةُ إيصالاً
            // لردّ عميلها ولا تُلزِم فاتورةً لردّها على مورّدها.
            'require_return_source' => false,
            // مثيله في `sales` — قد تضع المؤسسة مهلةً لعملائها ولا تخضع
            // لمهلةٍ مع مورّديها (أو العكس).
            'return_window_days'    => 0,
        ],
        /**
         * سياسات تشغيل الخزائن والحسابات البنكية.
         *
         * `allow_negative_transfer_balance` سياسة عمل: مؤسسات تسمح بالسحب على
         * المكشوف أو تسوية الرصيد لاحقاً، وأخرى تمنع ذلك. الافتراض `false`
         * يحمي النقد الافتراضي ولا يُغيّر إعداد مستأجر قائم إن اختار `true`.
         */
        'finance' => [
            'allow_negative_transfer_balance' => false,
            // تعطيل افتراضي لحماية المستأجرين القائمين إلى أن يقرّروا التفعيل صراحةً.
            'financial_alerts_enabled'        => false,
            // نافذة التقارير الدورية بالأيام؛ لا تؤثر في القيد أو الترحيل.
            'alert_check_window_days'         => 1,
        ],
        /**
         * سياسة تعديل البُعد التحليلي للتصنيف.
         *
         * التصنيف لا يغيّر حساباً أو مبلغاً أو قيداً، لكن تعديله بعد الترحيل قد
         * يغيّر تجميع التقرير لفترة سابقة؛ لذلك المنع هو الافتراض الحامي.
         * أمّا المسودة فلا قيد لها، فيبقى تعديلها مسموحاً افتراضياً.
         */
        'reports' => [
            'allow_classification_edit_before_post' => true,
            'allow_classification_edit_after_post'  => false,
        ],

        /**
         * ═══════════════════════════════════════════════════════════
         *  الفوترة الإلكترونية — نطاق عدّاد ICV
         * ═══════════════════════════════════════════════════════════
         *  **هذا الإعداد لا علاقة له ببادئات ترقيم المستندات.** أرقام
         *  المستندات (`invoice_prefix` وأخواتها) تُولَّد من طبقة
         *  `GeneratesDocumentNumbers` بقواعدها، و`zatca_icv` عدّادٌ مستقلٌّ
         *  تماماً بقواعده. الخلط بينهما هو ما يجب أن يمنعه هذا التعليق.
         *
         *  ⚠️ **تحذير قبل تغيير `icv_scope` إلى `branch`:**
         *  ICV عدّاد سلسلة إصدارٍ مرتبطٌ **برقم ضريبي مسجَّل لدى الهيئة**.
         *  تفعيلُه لفروعٍ تشترك في رقمٍ ضريبيٍّ واحد يُنتج عدة سلاسل ICV
         *  متوازية تحت الرقم نفسه — وهو مخالفٌ لمواصفة ZATCA ويعرّض الفواتير
         *  للرفض عند تفعيل المرحلة الثانية. ولذلك **لا يكفي تغيير هذه القيمة**:
         *  التوفّر محكومٌ بحارس في `ZatcaIcvScope` يتحقّق من شرطين تقنيين
         *  معاً، ويتجاهل القيمة المخزَّنة إن لم يتحقّقا.
         *
         *  **الافتراض `tenant` ولا يتغيّر تلقائياً لأي مستأجر — قائمٍ أو جديد.**
         */
        'zatca' => [
            'icv_scope' => 'tenant', // tenant | branch — انظر ZatcaIcvScope
        ],

        /**
         * بادئات الترقيم التي لا تخصّ وحدةً بعينها.
         *
         * **مجموعة مستقلّة عمداً، ولا تُدمج في `branches`:** ذلك المفتاح في
         * `tenants.settings` يملكه `BranchSettings` وحده، وله سلوك كتابة
         * مغاير (يحفظ المفاتيح المجهولة). و`Settings::put` يستبدل المجموعة
         * بمفاتيحه المعروفة فقط — فلو تقاسما المفتاح لمحا أحدُهما مفاتيح
         * الآخر صامتاً. الفصل هنا يمنع ذلك بنيوياً.
         *
         * الافتراض فارغ = بلا بادئة، وهو سلوك اليوم حرفياً (`00001`).
         */
        'numbering' => [
            'branch_prefix'    => '',
            'warehouse_prefix' => '',
        ],
    ];

    /** كل إعدادات مجموعة، مدموجةً فوق افتراضاتها. */
    public static function group(string $group, ?Tenant $tenant = null): array
    {
        $defaults = self::DEFAULTS[$group] ?? [];
        $tenant ??= self::tenant();

        if (! $tenant) {
            return $defaults;
        }

        // المفاتيح المجهولة تُهمَل: الافتراضات هي العقد.
        $stored = array_intersect_key($tenant->settings[$group] ?? [], $defaults);

        return array_merge($defaults, $stored);
    }

    /** قيمة إعداد واحد. */
    public static function get(string $group, string $key): mixed
    {
        return self::group($group)[$key] ?? null;
    }

    /** دمج المُدخل فوق القائم وحفظه. المفاتيح غير المرسلة تبقى كما هي. */
    public static function put(string $group, array $values, ?Tenant $tenant = null): array
    {
        $tenant ??= self::tenant();
        if (! $tenant) {
            return self::group($group);
        }

        $merged  = array_merge(self::group($group, $tenant), array_filter(
            array_intersect_key($values, self::DEFAULTS[$group] ?? []),
            fn ($v) => $v !== null
        ));

        $settings          = $tenant->settings ?? [];
        $settings[$group]  = $merged;
        $tenant->update(['settings' => $settings]);

        return $merged;
    }

    /** المستأجر النشط، أو null خارج سياق مستأجر (أوامر artisan). */
    private static function tenant(): ?Tenant
    {
        $id = app(TenantContext::class)->id();

        return $id ? Tenant::find($id) : null;
    }
}
